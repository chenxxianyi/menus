# 前端开发任务文档

## 项目概述

本项目包含两个前端子项目：
- **web-user** - 用户端响应式 Web App（优先适配移动端和 Pad 端）
- **admin-web** - 管理后台

---

## 一、技术栈

### web-user 用户端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.4 | 核心框架 |
| Vite | ^5.0 | 构建工具 |
| TypeScript | ^5.0 | 类型系统 |
| Pinia | ^2.1 | 状态管理 |
| Vue Router | ^4.2 | 路由管理 |
| Axios | ^1.6 | HTTP 请求 |
| Tailwind CSS | ^3.4 | 样式框架 |
| Vant | ^4.8 | 移动端 UI 组件库 |

### admin-web 管理后台

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.4 | 核心框架 |
| Vite | ^5.0 | 构建工具 |
| TypeScript | ^5.0 | 类型系统 |
| Element Plus | ^2.5 | UI 组件库 |
| Pinia | ^2.1 | 状态管理 |
| Vue Router | ^4.2 | 路由管理 |
| Axios | ^1.6 | HTTP 请求 |

---

## 二、项目结构

### web-user 目录结构

```
web-user/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                    # API 接口封装
│   │   ├── index.ts            # Axios 实例配置
│   │   ├── auth.ts             # 认证相关接口
│   │   ├── recipe.ts           # 菜谱相关接口
│   │   ├── recommend.ts        # 推荐相关接口
│   │   ├── user.ts             # 用户相关接口
│   │   ├── shopping.ts         # 购物清单接口
│   │   └── home.ts             # 首页接口
│   ├── assets/                 # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       └── global.css      # 全局样式
│   ├── components/             # 公共组件
│   │   ├── AppLayout.vue       # 应用布局
│   │   ├── BottomTabBar.vue    # 底部导航栏
│   │   ├── RecipeCard.vue      # 菜谱卡片
│   │   ├── MenuCard.vue        # 菜单卡片
│   │   ├── IngredientTag.vue   # 食材标签
│   │   ├── FilterDrawer.vue    # 筛选抽屉
│   │   ├── EmptyState.vue      # 空状态
│   │   ├── LoadingState.vue    # 加载状态
│   │   └── ResponsiveContainer.vue  # 响应式容器
│   ├── composables/            # 组合式函数
│   │   ├── useAuth.ts
│   │   ├── useResponsive.ts
│   │   └── useFavorite.ts
│   ├── pages/                  # 页面组件
│   │   ├── Home.vue            # 首页
│   │   ├── RecipeList.vue      # 菜谱列表
│   │   ├── RecipeDetail.vue    # 菜谱详情
│   │   ├── Recommend.vue       # 智能菜单推荐
│   │   ├── RecommendResult.vue # 推荐结果
│   │   ├── IngredientRecommend.vue  # 按食材推荐
│   │   ├── WeekMenu.vue        # 一周菜单
│   │   ├── ShoppingList.vue    # 购物清单
│   │   ├── Favorite.vue        # 我的收藏
│   │   ├── UserCenter.vue      # 用户中心
│   │   ├── Preferences.vue     # 偏好设置
│   │   ├── Feedback.vue        # 意见反馈
│   │   ├── Login.vue           # 登录
│   │   └── Register.vue        # 注册
│   ├── router/                 # 路由配置
│   │   └── index.ts
│   ├── stores/                 # Pinia 状态
│   │   ├── user.ts             # 用户状态
│   │   ├── recipe.ts           # 菜谱状态
│   │   └── shopping.ts         # 购物清单状态
│   ├── types/                  # TypeScript 类型
│   │   ├── api.ts              # API 响应类型
│   │   ├── recipe.ts           # 菜谱类型
│   │   ├── user.ts             # 用户类型
│   │   └── recommend.ts        # 推荐类型
│   ├── utils/                  # 工具函数
│   │   ├── storage.ts          # 本地存储
│   │   ├── format.ts           # 格式化
│   │   └── validate.ts         # 验证
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── postcss.config.js
```

### admin-web 目录结构

```
admin-web/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                    # API 接口封装
│   │   ├── index.ts
│   │   ├── auth.ts
│   │   ├── recipe.ts
│   │   ├── category.ts
│   │   ├── ingredient.ts
│   │   ├── banner.ts
│   │   ├── user.ts
│   │   ├── dashboard.ts
│   │   └── feedback.ts
│   ├── assets/
│   │   └── styles/
│   │       └── global.css
│   ├── components/             # 公共组件
│   │   ├── AdminLayout.vue     # 后台布局
│   │   ├── Sidebar.vue         # 侧边栏
│   │   ├── Header.vue          # 顶部栏
│   │   └── Breadcrumb.vue      # 面包屑
│   ├── pages/                  # 页面组件
│   │   ├── Login.vue           # 登录页
│   │   ├── Dashboard.vue       # 数据概览
│   │   ├── RecipeManage.vue    # 菜谱管理
│   │   ├── RecipeEdit.vue      # 菜谱编辑
│   │   ├── CategoryManage.vue  # 分类管理
│   │   ├── IngredientManage.vue # 食材管理
│   │   ├── BannerManage.vue    # 轮播图管理
│   │   ├── UserManage.vue      # 用户管理
│   │   ├── RecommendLogs.vue   # 推荐记录
│   │   └── FeedbackManage.vue  # 反馈管理
│   ├── router/
│   │   └── index.ts
│   ├── stores/
│   │   ├── admin.ts
│   │   └── app.ts
│   ├── types/
│   │   └── index.ts
│   ├── utils/
│   │   └── index.ts
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 三、响应式设计要求

### 断点定义

| 断点 | 范围 | 设备 |
|------|------|------|
| mobile | < 768px | 手机 |
| tablet | 768px - 1199px | 平板 |
| desktop | >= 1200px | 桌面 |

### Tailwind CSS 配置

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    screens: {
      'sm': '640px',
      'md': '768px',
      'lg': '1024px',
      'xl': '1200px',
      '2xl': '1536px',
    },
    extend: {
      colors: {
        primary: {
          50: '#f0fdf4',
          100: '#dcfce7',
          200: '#bbf7d0',
          300: '#86efac',
          400: '#4ade80',
          500: '#22c55e',
          600: '#16a34a',
          700: '#15803d',
          800: '#166534',
          900: '#14532d',
        },
        accent: {
          50: '#fff7ed',
          100: '#ffedd5',
          200: '#fed7aa',
          300: '#fdba74',
          400: '#fb923c',
          500: '#f97316',
          600: '#ea580c',
          700: '#c2410c',
          800: '#9a3412',
          900: '#7c2d12',
        },
      },
    },
  },
}
```

### 移动端设计规范

- 单列布局
- 底部 TabBar 导航（首页、菜谱、推荐、购物清单、我的）
- 卡片式菜谱列表，圆角 12px，阴影轻柔
- 筛选条件使用 Vant 抽屉弹层
- 推荐表单分步骤展示
- 底部固定主操作按钮，高度 56px
- 最小触控区域 44px × 44px

### Pad 端设计规范

- 双栏或三栏布局
- 左侧显示分类/筛选栏（宽度 280px）
- 右侧显示菜谱内容
- 菜谱详情采用图文分栏
- 一周菜单使用横向卡片布局

### 桌面端设计规范

- 最大内容宽度 1200px
- 中心化布局
- 顶部导航栏
- 可展示更多信息密度

---

## 四、页面开发任务

### 4.1 首页 (Home.vue)

**任务描述**：展示今日推荐、快捷入口、热门内容

**功能清单**：
- [ ] 顶部搜索框（支持跳转到菜谱列表页）
- [ ] 轮播图 Banner（调用首页接口）
- [ ] 今日推荐菜单卡片
- [ ] 早餐/午餐/晚餐快捷推荐入口
- [ ] 热门菜谱横向滚动列表
- [ ] 菜谱分类宫格入口
- [ ] AI 菜单推荐入口卡片
- [ ] 一周菜单入口卡片
- [ ] 购物清单入口卡片

**接口依赖**：
- `GET /api/home` - 获取首页数据
- `GET /api/banners` - 获取轮播图

**组件拆分**：
- `SearchBar.vue` - 搜索框
- `BannerSwiper.vue` - 轮播图
- `QuickEntry.vue` - 快捷入口
- `HotRecipes.vue` - 热门菜谱

---

### 4.2 菜谱列表 (RecipeList.vue)

**任务描述**：菜谱搜索和筛选列表

**功能清单**：
- [ ] 搜索框（搜索菜名/食材）
- [ ] 分类筛选（菜谱分类）
- [ ] 口味筛选（咸鲜/酸甜/麻辣/清淡等）
- [ ] 烹饪时间筛选（<15分钟/15-30分钟/30-60分钟/>60分钟）
- [ ] 难度筛选（简单/中等/困难）
- [ ] 健康标签筛选（低脂/高蛋白/素食等）
- [ ] 移动端抽屉筛选（FilterDrawer 组件）
- [ ] Pad 端左侧筛选栏
- [ ] 菜谱卡片列表（RecipeCard 组件）
- [ ] 下拉刷新
- [ ] 上拉加载更多
- [ ] 空状态展示

**接口依赖**：
- `GET /api/recipes` - 获取菜谱列表（支持分页、筛选参数）
- `GET /api/categories` - 获取分类列表

**筛选参数**：
```typescript
interface RecipeQuery {
  keyword?: string;
  category_id?: number;
  taste?: string;
  cook_time?: string;
  difficulty?: string;
  health_tags?: string;
  page?: number;
  page_size?: number;
}
```

---

### 4.3 菜谱详情 (RecipeDetail.vue)

**任务描述**：展示菜谱完整信息

**功能清单**：
- [ ] 封面图展示（大图）
- [ ] 菜名、简介
- [ ] 基本信息（适合人数、烹饪时间、难度）
- [ ] 食材清单（带数量）
- [ ] 调料清单
- [ ] 详细步骤（带图片）
- [ ] 小技巧提示
- [ ] 营养信息展示
- [ ] 收藏按钮（支持取消收藏）
- [ ] 加入购物清单按钮
- [ ] 分享链接功能
- [ ] 底部固定操作栏

**接口依赖**：
- `GET /api/recipes/{id}` - 获取菜谱详情
- `POST /api/recipes/{id}/favorite` - 收藏
- `DELETE /api/recipes/{id}/favorite` - 取消收藏
- `POST /api/shopping-list` - 添加到购物清单

---

### 4.4 智能菜单推荐 (Recommend.vue)

**任务描述**：用户填写条件，系统推荐菜单

**功能清单**：
- [ ] 步骤一：用餐人数选择（1-10人）
- [ ] 步骤二：用餐时间选择（早餐/午餐/晚餐）
- [ ] 步骤三：口味偏好选择（多选）
- [ ] 步骤四：饮食目标选择（普通/减脂/增肌/控糖/儿童营养）
- [ ] 步骤五：忌口食材输入（标签式输入）
- [ ] 步骤六：已有食材输入（标签式输入）
- [ ] 步骤七：期望烹饪时间选择
- [ ] 提交推荐请求
- [ ] 加载状态展示

**接口依赖**：
- `POST /api/recommend/menu` - 获取推荐菜单

**请求参数**：
```typescript
interface RecommendRequest {
  people_count: number;
  meal_type: 'breakfast' | 'lunch' | 'dinner';
  taste_preference: string[];
  health_goal: string;
  avoid_ingredients: string[];
  existing_ingredients: string[];
  cook_time_preference: string;
}
```

---

### 4.5 推荐结果 (RecommendResult.vue)

**任务描述**：展示推荐菜单结果

**功能清单**：
- [ ] 菜单名称展示
- [ ] 推荐理由
- [ ] 主菜/配菜/汤/主食搭配列表
- [ ] 每道菜的制作时间和难度
- [ ] 食材汇总列表
- [ ] 一键加入购物清单
- [ ] 保存菜单功能
- [ ] 重新推荐按钮

**接口依赖**：
- `POST /api/shopping-list` - 添加到购物清单
- `POST /api/menus` - 保存菜单

---

### 4.6 按食材推荐 (IngredientRecommend.vue)

**任务描述**：根据已有食材推荐菜谱

**功能清单**：
- [ ] 食材输入框（标签式输入）
- [ ] 常用食材快捷选择
- [ ] 推荐菜谱列表
- [ ] 食材匹配度展示
- [ ] 缺少食材提醒
- [ ] 跳转菜谱详情

**接口依赖**：
- `POST /api/recommend/by-ingredients` - 按食材推荐

---

### 4.7 一周菜单 (WeekMenu.vue)

**任务描述**：展示和管理一周菜单

**功能清单**：
- [ ] 7天菜单横向展示（移动端纵向）
- [ ] 每天包含早餐/午餐/晚餐
- [ ] 重新生成单日菜单
- [ ] 保存一周菜单
- [ ] 生成购物清单
- [ ] 菜品可点击跳转详情

**接口依赖**：
- `POST /api/recommend/week-menu` - 生成一周菜单
- `POST /api/menus` - 保存菜单

---

### 4.8 购物清单 (ShoppingList.vue)

**任务描述**：管理购物清单

**功能清单**：
- [ ] 食材分类展示（蔬菜/肉类/调料等）
- [ ] 勾选已购买
- [ ] 手动添加食材
- [ ] 删除单个食材
- [ ] 清空全部
- [ ] 已购买/未购买切换显示

**接口依赖**：
- `GET /api/shopping-list` - 获取购物清单
- `PUT /api/shopping-list/{id}` - 更新购物清单
- `DELETE /api/shopping-list/{id}` - 删除购物清单

---

### 4.9 用户中心 (UserCenter.vue)

**任务描述**：用户个人信息和功能入口

**功能清单**：
- [ ] 用户头像和昵称展示
- [ ] 登录/注册入口（未登录状态）
- [ ] 个人资料入口
- [ ] 饮食偏好设置入口
- [ ] 我的收藏入口
- [ ] 我的菜单入口
- [ ] 浏览历史入口
- [ ] 我的购物清单入口
- [ ] 意见反馈入口
- [ ] 退出登录

---

### 4.10 偏好设置 (Preferences.vue)

**任务描述**：设置用户饮食偏好

**功能清单**：
- [ ] 口味偏好选择（多选）
- [ ] 饮食目标选择
- [ ] 忌口食材设置
- [ ] 喜欢的食材设置
- [ ] 烹饪时间偏好
- [ ] 常用用餐人数
- [ ] 保存设置

**接口依赖**：
- `GET /api/user/preferences` - 获取偏好
- `PUT /api/user/preferences` - 更新偏好

---

### 4.11 登录/注册 (Login.vue / Register.vue)

**任务描述**：用户认证页面

**功能清单**：
- [ ] 登录表单（用户名/密码）
- [ ] 注册表单（用户名/邮箱/密码/确认密码）
- [ ] 表单验证
- [ ] 登录成功跳转
- [ ] Token 存储
- [ ] 记住登录状态

**接口依赖**：
- `POST /api/auth/login` - 登录
- `POST /api/auth/register` - 注册

---

### 4.12 意见反馈 (Feedback.vue)

**任务描述**：提交用户反馈

**功能清单**：
- [ ] 反馈内容输入
- [ ] 联系方式输入（可选）
- [ ] 提交反馈
- [ ] 提交成功提示

**接口依赖**：
- `POST /api/feedback` - 提交反馈

---

## 五、公共组件开发任务

### 5.1 AppLayout.vue

**功能**：应用整体布局

```vue
<template>
  <div class="app-layout">
    <router-view />
    <BottomTabBar v-if="showTabBar" />
  </div>
</template>
```

- 判断是否显示底部导航栏
- 路由变化时滚动到顶部

---

### 5.2 BottomTabBar.vue

**功能**：底部导航栏

**Tab 项**：
| Tab | 图标 | 路由 |
|-----|------|------|
| 首页 | home | / |
| 菜谱 | recipe | /recipes |
| 推荐 | recommend | /recommend |
| 购物清单 | cart | /shopping-list |
| 我的 | user | /user |

**要求**：
- 使用 Vant Tabbar 组件
- 选中状态高亮
- 支持角标提示

---

### 5.3 RecipeCard.vue

**功能**：菜谱卡片组件

**Props**：
```typescript
interface RecipeCardProps {
  id: number;
  title: string;
  cover: string;
  cookTime: number;
  difficulty: string;
  taste: string;
  favoriteCount: number;
}
```

**展示内容**：
- 封面图
- 菜名
- 烹饪时间
- 难度标签
- 收藏数

---

### 5.4 FilterDrawer.vue

**功能**：筛选抽屉组件

**Props**：
```typescript
interface FilterDrawerProps {
  visible: boolean;
  categories: Category[];
  filters: FilterOptions;
}
```

**功能**：
- 分类筛选
- 口味筛选
- 烹饪时间筛选
- 难度筛选
- 健康标签筛选
- 重置筛选
- 确认筛选

---

### 5.5 EmptyState.vue

**功能**：空状态组件

**Props**：
```typescript
interface EmptyStateProps {
  type: 'no-data' | 'no-result' | 'no-favorite' | 'no-history';
  text?: string;
}
```

---

### 5.6 LoadingState.vue

**功能**：加载状态组件

**支持模式**：
- 骨架屏
- 加载动画
- 全屏加载

---

## 六、API 封装任务

### 6.1 Axios 实例配置

```typescript
// api/index.ts
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
});

// 请求拦截器
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器
api.interceptors.response.use(
  (response) => {
    const { code, message, data } = response.data;
    if (code === 0) {
      return data;
    }
    return Promise.reject(new Error(message));
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

### 6.2 接口模块划分

| 模块 | 文件 | 接口数量 |
|------|------|----------|
| 认证 | auth.ts | 2 |
| 首页 | home.ts | 1 |
| 菜谱 | recipe.ts | 4 |
| 推荐 | recommend.ts | 3 |
| 用户 | user.ts | 5 |
| 购物清单 | shopping.ts | 4 |
| 反馈 | feedback.ts | 1 |

---

## 七、状态管理任务

### 7.1 用户状态 (stores/user.ts)

```typescript
interface UserState {
  token: string | null;
  userInfo: UserInfo | null;
  isLoggedIn: boolean;
}
```

**Actions**：
- login - 登录
- register - 注册
- logout - 退出登录
- getUserInfo - 获取用户信息
- updateProfile - 更新个人资料

---

### 7.2 菜谱状态 (stores/recipe.ts)

```typescript
interface RecipeState {
  categories: Category[];
  currentRecipe: Recipe | null;
  favorites: number[];
}
```

**Actions**：
- fetchCategories - 获取分类
- fetchRecipeDetail - 获取菜谱详情
- toggleFavorite - 切换收藏状态

---

### 7.3 购物清单状态 (stores/shopping.ts)

```typescript
interface ShoppingState {
  lists: ShoppingList[];
  currentList: ShoppingItem[];
}
```

**Actions**：
- fetchLists - 获取购物清单
- addItem - 添加项目
- removeItem - 删除项目
- toggleChecked - 切换勾选状态

---

## 八、路由配置任务

### 8.1 路由表

```typescript
const routes = [
  {
    path: '/',
    component: AppLayout,
    children: [
      { path: '', name: 'Home', component: () => import('@/pages/Home.vue') },
      { path: 'recipes', name: 'RecipeList', component: () => import('@/pages/RecipeList.vue') },
      { path: 'recipes/:id', name: 'RecipeDetail', component: () => import('@/pages/RecipeDetail.vue') },
      { path: 'recommend', name: 'Recommend', component: () => import('@/pages/Recommend.vue') },
      { path: 'recommend/result', name: 'RecommendResult', component: () => import('@/pages/RecommendResult.vue') },
      { path: 'ingredient-recommend', name: 'IngredientRecommend', component: () => import('@/pages/IngredientRecommend.vue') },
      { path: 'week-menu', name: 'WeekMenu', component: () => import('@/pages/WeekMenu.vue') },
      { path: 'shopping-list', name: 'ShoppingList', component: () => import('@/pages/ShoppingList.vue') },
      { path: 'user', name: 'UserCenter', component: () => import('@/pages/UserCenter.vue') },
      { path: 'user/preferences', name: 'Preferences', component: () => import('@/pages/Preferences.vue') },
      { path: 'user/favorites', name: 'Favorite', component: () => import('@/pages/Favorite.vue') },
      { path: 'feedback', name: 'Feedback', component: () => import('@/pages/Feedback.vue') },
    ],
  },
  { path: '/login', name: 'Login', component: () => import('@/pages/Login.vue') },
  { path: '/register', name: 'Register', component: () => import('@/pages/Register.vue') },
];
```

### 8.2 路由守卫

- 未登录用户只能访问：首页、菜谱列表、菜谱详情、登录、注册
- 需要登录的页面：用户中心、偏好设置、收藏、购物清单、推荐、反馈

---

## 九、UI 风格指南

### 9.1 色彩规范

| 用途 | 颜色 | 色值 |
|------|------|------|
| 主色 | 自然绿色 | #16a34a |
| 主色-浅 | 浅绿 | #dcfce7 |
| 辅色 | 暖橙色 | #f97316 |
| 辅色-浅 | 浅橙 | #ffedd5 |
| 背景色 | 米白 | #fafaf9 |
| 卡片背景 | 白色 | #ffffff |
| 文字-主 | 深灰 | #1c1917 |
| 文字-次 | 中灰 | #78716c |
| 文字-辅 | 浅灰 | #a8a29e |
| 边框 | 浅灰 | #e7e5e4 |
| 成功 | 绿色 | #22c55e |
| 警告 | 橙色 | #f59e0b |
| 错误 | 红色 | #ef4444 |

### 9.2 字体规范

```css
/* 全局字体 */
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

/* 字号 */
--text-xs: 12px;    /* 辅助文字 */
--text-sm: 14px;    /* 次要文字 */
--text-base: 16px;  /* 正文 */
--text-lg: 18px;    /* 小标题 */
--text-xl: 20px;    /* 标题 */
--text-2xl: 24px;   /* 大标题 */
--text-3xl: 30px;   /* 特大标题 */
```

### 9.3 间距规范

```css
/* 间距 */
--spacing-1: 4px;
--spacing-2: 8px;
--spacing-3: 12px;
--spacing-4: 16px;
--spacing-5: 20px;
--spacing-6: 24px;
--spacing-8: 32px;
--spacing-10: 40px;
--spacing-12: 48px;
```

### 9.4 圆角规范

```css
/* 圆角 */
--radius-sm: 4px;    /* 小组件 */
--radius-md: 8px;    /* 按钮、输入框 */
--radius-lg: 12px;   /* 卡片 */
--radius-xl: 16px;   /* 大卡片、弹窗 */
--radius-full: 9999px; /* 圆形 */
```

### 9.5 阴影规范

```css
/* 阴影 */
--shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
--shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
--shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
```

---

## 十、admin-web 管理后台开发任务

### 10.1 登录页 (Login.vue)

- [ ] 登录表单（用户名/密码）
- [ ] 表单验证
- [ ] 登录接口调用
- [ ] Token 存储
- [ ] 跳转到 Dashboard

---

### 10.2 数据概览 (Dashboard.vue)

- [ ] 用户数统计卡片
- [ ] 菜谱数统计卡片
- [ ] 推荐次数统计卡片
- [ ] 收藏数统计卡片
- [ ] 近7天用户增长趋势图
- [ ] 近7天推荐次数趋势图
- [ ] 热门菜谱排行

**接口依赖**：
- `GET /api/admin/dashboard`

---

### 10.3 菜谱管理 (RecipeManage.vue / RecipeEdit.vue)

**列表页**：
- [ ] 菜谱列表表格
- [ ] 搜索功能
- [ ] 分类筛选
- [ ] 状态筛选（上架/下架）
- [ ] 新增按钮
- [ ] 编辑按钮
- [ ] 删除按钮
- [ ] 上架/下架切换
- [ ] 分页

**编辑页**：
- [ ] 表单：菜名、封面、简介
- [ ] 表单：分类、烹饪时间、难度、人数
- [ ] 表单：口味、健康标签
- [ ] 食材清单编辑（动态增减）
- [ ] 调料清单编辑（动态增减）
- [ ] 步骤编辑（支持图片上传）
- [ ] 小技巧
- [ ] 营养信息
- [ ] 保存/取消

**接口依赖**：
- `GET /api/admin/recipes` - 列表
- `POST /api/admin/recipes` - 新增
- `PUT /api/admin/recipes/{id}` - 编辑
- `DELETE /api/admin/recipes/{id}` - 删除

---

### 10.4 分类管理 (CategoryManage.vue)

- [ ] 分类列表表格
- [ ] 新增分类（名称、图标、排序）
- [ ] 编辑分类
- [ ] 删除分类
- [ ] 状态切换

**接口依赖**：
- `GET /api/admin/categories`
- `POST /api/admin/categories`
- `PUT /api/admin/categories/{id}`
- `DELETE /api/admin/categories/{id}`

---

### 10.5 食材管理 (IngredientManage.vue)

- [ ] 食材列表表格
- [ ] 搜索功能
- [ ] 分类筛选
- [ ] 新增食材（名称、分类、图片、营养信息）
- [ ] 编辑食材
- [ ] 删除食材

**接口依赖**：
- `GET /api/admin/ingredients`
- `POST /api/admin/ingredients`
- `PUT /api/admin/ingredients/{id}`
- `DELETE /api/admin/ingredients/{id}`

---

### 10.6 轮播图管理 (BannerManage.vue)

- [ ] 轮播图列表
- [ ] 新增（标题、图片、链接类型、链接值、排序）
- [ ] 编辑
- [ ] 删除
- [ ] 状态切换
- [ ] 图片上传

**接口依赖**：
- `GET /api/admin/banners`
- `POST /api/admin/banners`
- `PUT /api/admin/banners/{id}`
- `DELETE /api/admin/banners/{id}`

---

### 10.7 用户管理 (UserManage.vue)

- [ ] 用户列表表格
- [ ] 搜索功能
- [ ] 用户详情查看
- [ ] 用户偏好查看

**接口依赖**：
- `GET /api/admin/users`

---

### 10.8 推荐记录 (RecommendLogs.vue)

- [ ] 推荐记录列表
- [ ] 推荐类型筛选
- [ ] 查看推荐详情（输入/输出）

**接口依赖**：
- `GET /api/admin/recommend-logs`

---

### 10.9 反馈管理 (FeedbackManage.vue)

- [ ] 反馈列表
- [ ] 状态筛选（待处理/已处理）
- [ ] 查看反馈详情
- [ ] 标记为已处理

**接口依赖**：
- `GET /api/admin/feedback`
- `PUT /api/admin/feedback/{id}`

---

## 十一、开发优先级

### P0 - 核心功能（第1-2周）

1. 项目初始化和基础配置
2. API 封装和类型定义
3. 用户认证（登录/注册）
4. 首页
5. 菜谱列表
6. 菜谱详情

### P1 - 重要功能（第3-4周）

7. 智能菜单推荐
8. 推荐结果
9. 购物清单
10. 收藏功能

### P2 - 增强功能（第5-6周）

11. 按食材推荐
12. 一周菜单
13. 偏好设置
14. 用户中心完善

### P3 - 管理后台（第7-8周）

15. 管理后台登录
16. Dashboard
17. 菜谱管理
18. 分类管理
19. 其他管理功能

---

## 十二、验收标准

### 功能验收

- [ ] 所有页面功能正常
- [ ] API 调用正常
- [ ] 错误处理完善
- [ ] 加载状态展示

### 响应式验收

- [ ] 移动端（375px）布局正常
- [ ] Pad 端（768px）布局正常
- [ ] 桌面端（1200px）布局正常
- [ ] 触控区域符合规范

### 性能验收

- [ ] 首屏加载 < 3秒
- [ ] 页面切换流畅
- [ ] 图片懒加载
- [ ] 列表虚拟滚动（长列表）

### 代码质量

- [ ] TypeScript 无类型错误
- [ ] ESLint 无警告
- [ ] 组件拆分合理
- [ ] 代码注释完善

---

## 十三、开发环境

### 环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8000/api
VITE_APP_TITLE=菜单推荐

# .env.production
VITE_API_BASE_URL=https://api.example.com/api
VITE_APP_TITLE=菜单推荐
```

### 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 代码检查
npm run lint

# 类型检查
npm run type-check
```

---

## 十四、注意事项

1. **Mobile First**：所有页面先实现移动端布局，再适配 Pad 和桌面端
2. **组件复用**：公共组件抽取到位，避免重复代码
3. **类型安全**：充分利用 TypeScript，API 响应和组件 Props 都要有类型定义
4. **错误处理**：网络请求错误、业务错误都要有友好提示
5. **加载状态**：所有异步操作都要有加载状态
6. **空状态**：列表为空时要有空状态展示
7. **性能优化**：图片懒加载、路由懒加载、长列表优化
