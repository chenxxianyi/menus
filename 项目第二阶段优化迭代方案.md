# 今天吃什么项目第二阶段优化迭代方案

版本：v2.0  
日期：2026-07-10  
适用范围：`web-user` 用户端、`backend` 后端服务、推荐系统、数据统计、部署与运维  
关联文档：[产品迭代方案.md](./产品迭代方案.md)、[迭代开发任务.md](./迭代开发任务.md)

## 1. 迭代背景

项目当前已经具备以下核心能力：

- 用户偏好与个性化推荐
- 菜谱浏览、收藏和历史记录
- AI 菜谱与菜单生成
- 一周菜单规划
- 购物清单生成与合并
- 菜谱反馈与行为事件记录
- 情侣绑定与共同点餐
- 菜单保存和复用

产品已经完成了从“菜谱工具”到“家庭用餐决策助手”的基础能力建设。第二阶段不再以增加独立页面和功能入口为主要目标，而是重点解决以下问题：

1. 部分界面存在看似可操作、实际未完成的交互，影响用户信任。
2. 前端缺少自动化测试、代码检查和持续集成，迭代回归风险较高。
3. 生产环境配置、接口限流和 AI 成本控制仍需加强。
4. 用户行为、做饭结果和购物完成情况尚未充分反哺推荐系统。
5. 数据统计页缺少真实营养和饮食趋势数据。
6. 产品已有事件数据，但缺少漏斗、留存和异常监控体系。

本轮目标是让项目从“功能完整”提升为“稳定、可信、可持续运营”。

## 2. 核心目标

### 2.1 产品目标

- 用户点击的每个操作都能得到明确、真实的结果。
- 用户可以顺畅完成“推荐 -> 菜谱 -> 菜单 -> 购物 -> 做饭 -> 反馈”闭环。
- 推荐结果能够解释原因，并根据真实行为持续优化。
- 统计数据能够帮助用户理解饮食习惯，而不是只展示功能使用次数。

### 2.2 工程目标

- 建立前端测试、类型检查、代码规范和持续集成流程。
- 消除生产环境中的默认密钥、调试配置和高风险接口。
- 建立关键接口的性能、错误率和 AI 成本监控。
- 降低新功能开发对已有核心流程造成回归的概率。

### 2.3 建议核心指标

北极星指标：

> 每周完成的有效用餐决策数

“有效用餐决策”建议定义为用户完成以下任一行为：

- 将推荐菜谱加入一周菜单。
- 从菜谱或菜单生成购物清单。
- 将菜谱标记为已做。
- 对做过的菜谱提交反馈。

辅助指标：

| 指标 | 目标方向 |
| --- | --- |
| 首页到推荐页转化率 | 提升 |
| 推荐结果查看率 | 提升 |
| 菜谱加入菜单率 | 提升 |
| 菜单生成购物清单率 | 提升 |
| 购物清单完成率 | 提升 |
| 推荐空结果率 | 降低 |
| AI 生成失败率 | 降低 |
| AI 平均响应时间 | 降低或保持稳定 |
| 7 日留存率 | 提升 |
| 情侣绑定后协作使用率 | 提升 |

## 3. 迭代原则

1. 先修复无效交互，再增加新入口。
2. 先保证核心链路稳定，再扩展低频功能。
3. 用户反馈必须能够影响推荐结果。
4. 没有真实数据支撑的页面不长期保留空壳状态。
5. 所有异步操作都需要加载、成功、失败和重试状态。
6. 所有生产功能都需要考虑安全、限流、成本和可观测性。
7. 视觉继续遵循统一卡片、按钮、间距、阴影和状态规范，不再新增独立风格体系。

## 4. 迭代路线图

| 迭代 | 主题 | 优先级 | 建议周期 |
| --- | --- | --- | --- |
| Sprint 1 | 交互可信度与工程质量 | P0 | 1-2 周 |
| Sprint 2 | 推荐反馈闭环与真实统计 | P1 | 2 周 |
| Sprint 3 | 数据运营与长期留存 | P1/P2 | 2-3 周 |

## 4.1 本次开发完成状态（2026-07-10）

本次实现遵循“先 P0、再补可验证的 P1 基础能力”的范围。以下状态以代码、接口和本地验证结果为准；需要外部数据源、生产基础设施或实时通信的事项保留在后续 Sprint，不以模拟数据替代。

| 编号 | 状态 | 已完成内容 | 后续边界 |
| --- | --- | --- | --- |
| OPT-001 | 已完成 | 首页收藏/取消收藏、加载和失败提示；餐次切换按餐次重新请求推荐；移除随机 key；首页卡片与主要按钮可键盘访问。 | 菜谱库暂未维护独立餐次字段，当前餐次通过烹饪时长策略筛选并回退到可用菜谱。 |
| OPT-002 | 已完成 | 清除用户可见操作中的 `console.log`；购物清单、邀请码和联系邮箱均支持 Clipboard API 与兼容复制降级；无法复制/分享时给出可执行提示。 | 微信等第三方原生分享能力仍取决于浏览器/宿主环境。 |
| OPT-003 | 已完成基础规范 | 全局焦点样式、`prefers-reduced-motion`、44px 以上主触控目标、异步禁用状态及关键卡片键盘操作已落地。 | 继续在新增页面中复用该规范，并补充更完整的 Tooltip 组件。 |
| OPT-004 | 已完成基础门禁 | 引入 ESLint、Prettier、Vitest、Playwright 和 GitHub Actions；新增 `lint`、`format:check`、`type-check`、`test`、`test:e2e` 脚本；覆盖本地日期和复制降级单测，以及登录会话 E2E。 | 推荐、菜单、购物清单、反馈的完整 E2E 场景在后续继续补齐。Windows 本地 Playwright 启动器存在退出延迟，测试用例本身已通过，CI 在 Linux 环境执行。 |
| OPT-005 | 已完成核心安全项 | 新增生产配置校验、`.env.example`、环境区分、健康/就绪检查、优雅停机、请求 ID、敏感接口固定窗口限流、AI 并发限制、图片内容/像素校验。 | 限流当前为单实例内存实现；横向扩容时需换成 Redis 限流。Refresh Token 与 AI Token/费用精确统计待认证和模型响应协议确定后实施。 |
| OPT-006 | 已完成基础闭环 | 做过、喜欢、不喜欢、一般、太复杂、用时过长、食材难买和不再推荐均可在菜谱详情一键提交；反馈会进入推荐排序。 | 购物清单完成后主动询问“是否做过”、关联具体菜单 ID 需要后续扩展数据模型。 |
| OPT-007 | 已完成 | 推荐结果返回策略版本及可追溯理由；近期 14 天做过的菜谱显著降权，明确负反馈和屏蔽菜谱降权/排除。 | 需要真实线上实验数据后再调权重和策略版本。 |
| OPT-008 / OPT-009 | 已完成基础统计 | 新增真实饮食统计接口和统计页：周/月切换、已做次数、自炊天数、连续做饭、真实营养汇总、数据完整度、每日趋势和最近做过。 | 当前仅使用菜谱已有营养字段；食材权威营养库、份量换算和来源治理属于后续数据建设。 |
| OPT-010–OPT-013 | 未开始 | 保留原方案。 | 依赖运营看板、库存数据模型、模板设计和实时协作基础设施，不能以占位入口替代。 |

### 本次验证记录

- 后端：`go test ./...` 通过；新增限流、生产配置校验和统计计算测试。
- 前端：`npm run lint`、`npm run format:check`、`npm run type-check`、`npm run test`、`npm run build` 通过。
- E2E：登录会话场景在 Chromium 通过；Windows 下 Playwright 的 WebServer 子进程退出较慢，CI 工作流会在 Linux 环境复验。

### 发布前残余风险

- `npm audit --omit=dev` 当前将 `axios` 的传递依赖 `form-data` 标记为 2 个高危项，锁定依赖暂无自动修复版本。发布前应在测试环境验证可兼容的 Axios 升级或替代方案；在此之前，上传接口已在服务端实施文件类型、大小与内容校验，不依赖客户端请求体校验。

## 5. Sprint 1：交互可信度与工程质量

### 5.1 OPT-001 首页无效交互修复

优先级：P0  
涉及端：前端、后端接口复用

开发内容：

- 为首页收藏按钮接入真实收藏和取消收藏逻辑。
- 收藏操作增加加载态、成功提示、失败回滚和登录校验。
- 让早餐、午餐、晚餐等餐次切换真实影响推荐内容。
- 明确餐次无数据时的空状态和重新推荐入口。
- 移除使用 `Math.random()` 生成列表 key 的逻辑，改用稳定业务 ID。
- 检查首页“查看做法、换一组、加入清单”等按钮是否都能完整执行。

验收标准：

- 所有首页按钮点击后都有可观察的业务结果。
- 切换餐次后，推荐内容或筛选条件同步变化。
- 页面刷新和列表更新时不出现异常重渲染或状态错位。
- 请求失败时用户能够理解原因并重新尝试。

### 5.2 OPT-002 全站占位交互清理

优先级：P0  
涉及端：前端

重点范围：

- 用户中心资料和设置操作。
- 情侣绑定中的邀请码请求操作。
- 关于页面中的联系、版本等操作。
- 购物清单复制功能的降级处理。
- 其他只执行 `console.log`、空回调或仅阻止冒泡的按钮。

处理原则：

- 当前版本需要的功能：补齐真实逻辑。
- 暂时无法实现的功能：隐藏入口或标记为明确的不可用状态。
- 不允许保留“可以点击但没有结果”的控件。

验收标准：

- 全站不存在仅输出日志的用户操作。
- 禁用功能具备明确状态，并且不会误导用户。
- 复制、分享等浏览器能力失败时有可执行的替代方案。

### 5.3 OPT-003 统一交互状态规范

优先级：P0  
涉及端：前端

开发内容：

- 统一按钮默认、悬停、按下、禁用和加载状态。
- 图标按钮提供 `aria-label` 和 Tooltip。
- 主要触控目标不小于 44 x 44px。
- 表单错误显示在对应字段附近。
- 列表和卡片加载时预留稳定空间，减少内容跳动。
- 支持键盘焦点样式和 `prefers-reduced-motion`。
- 统一卡片圆角、边框、阴影和内部间距，限制特殊卡片变体数量。

验收标准：

- 375px、768px、1024px、1440px 宽度下无明显溢出和遮挡。
- 主要流程可以使用键盘完成。
- 页面不存在依赖颜色作为唯一状态提示的控件。
- 异步按钮请求期间不可重复提交。

### 5.4 OPT-004 前端质量门禁

优先级：P0  
涉及端：前端、CI

开发内容：

- 引入 ESLint、Prettier 和 `vue-tsc`。
- 引入 Vitest，优先覆盖 Store、API 封装、格式化工具和购物清单合并逻辑。
- 引入 Playwright，覆盖核心端到端路径：
  - 登录。
  - 获取推荐。
  - 查看菜谱。
  - 加入一周菜单。
  - 生成购物清单。
  - 提交做饭反馈。
- 增加统一脚本：`lint`、`format:check`、`type-check`、`test`、`test:e2e`。
- 增加 CI 工作流，自动执行前端检查、前端构建和 `go test ./...`。

验收标准：

- 合并代码前必须通过类型检查、单元测试和构建。
- 核心流程端到端测试可重复执行。
- CI 失败时能够定位到具体检查或测试。

### 5.5 OPT-005 生产环境安全加固

优先级：P0  
涉及端：后端、部署

开发内容：

- 数据库密码、JWT Secret、AI Key 等全部改为环境变量注入。
- 生产环境检测到默认 JWT Secret 时拒绝启动。
- 生产环境关闭 Debug 和自动迁移。
- 对登录、注册、邀请码、AI 生成、文件上传和反馈接口增加限流。
- 上传接口验证 MIME、扩展名、文件大小和文件内容类型。
- AI 请求增加用户配额、全局并发限制、超时和失败降级。
- 记录 AI 模型、Token 用量、耗时、状态和估算成本。
- 评估使用短期 Access Token 与 HttpOnly Refresh Token 的认证方案。

验收标准：

- 代码仓库和生产配置中不存在真实密钥。
- 高频重复请求能够被正确限制并返回明确错误。
- AI 服务不可用时不阻塞非 AI 核心功能。
- 安全配置具有开发、测试和生产环境区分。

## 6. Sprint 2：推荐反馈闭环与真实统计

### 6.1 OPT-006 做饭结果反馈

优先级：P1  
涉及端：前端、后端、数据库

开发内容：

- 菜谱详情和历史菜单增加“已做过”操作。
- 做饭完成后支持快速反馈：
  - 喜欢。
  - 一般。
  - 不喜欢。
  - 太复杂。
  - 用时过长。
  - 食材难买。
- 购物清单完成后询问是否完成对应菜谱。
- 反馈记录关联用户、菜谱、菜单、推荐来源和时间。

验收标准：

- 用户能够在不超过两步的操作中完成做饭反馈。
- 反馈数据能够被推荐服务读取。
- 同一道菜的重复反馈具有清晰的更新或追加规则。

### 6.2 OPT-007 推荐可解释与防重复

优先级：P1  
涉及端：推荐服务、前端

开发内容：

- 推荐结果展示简短推荐理由，例如：
  - 符合用户口味。
  - 使用现有食材。
  - 适合当前餐次。
  - 满足烹饪时长要求。
  - 近期没有吃过。
- 降低近期已做、明确不喜欢或连续展示菜谱的权重。
- 提供“为什么推荐”和“不想吃的原因”反馈入口。
- 推荐接口返回推荐来源、策略版本和理由编码。

验收标准：

- 每个个性化推荐结果至少有一个真实、可追溯的推荐理由。
- 用户标记不喜欢后，短期内不再高频出现同类结果。
- 推荐策略变更后能够通过版本字段分析效果。

### 6.3 OPT-008 营养数据基础建设

优先级：P1  
涉及端：后端、数据库、前端

开发内容：

- 建立食材营养数据来源和数据维护策略。
- 按菜谱份量计算热量、蛋白质、碳水和脂肪。
- 对缺失营养数据标记可信度或数据完整度。
- 后端统计接口返回按日、周和月聚合的数据。
- 数据不足时展示明确说明，不输出误导性的精确数值。

验收标准：

- 营养统计具有明确的数据来源和计算规则。
- 菜谱份量变化时营养数据同步变化。
- 缺失数据不会被当作零值参与错误统计。

### 6.4 OPT-009 重构统计页

优先级：P1  
涉及端：前端、后端

建议内容：

- 本周完成用餐决策数。
- 热量与三大营养素趋势。
- 食材丰富度。
- 自炊次数与连续做饭天数。
- 最常做和评分最高的菜谱。
- 推荐采纳率与购物清单完成率。

验收标准：

- 统计页不再只展示收藏、菜单和清单数量。
- 图表提供文本摘要或表格替代信息。
- 用户可以切换周、月等统计周期。
- 数据不足时给出有行动价值的引导。

## 7. Sprint 3：数据运营与长期留存

### 7.1 OPT-010 产品漏斗与运营看板

优先级：P1  
涉及端：后端、数据分析、内部管理界面

漏斗建议：

```text
进入首页
  -> 发起推荐
  -> 查看菜谱
  -> 加入菜单
  -> 生成购物清单
  -> 完成购物
  -> 标记已做
  -> 提交反馈
```

开发内容：

- 统一事件名称、属性和版本。
- 增加漏斗转化、用户留存和功能使用频率统计。
- 区分普通推荐、AI 推荐、食材推荐和情侣推荐来源。
- 监控推荐空结果率、接口错误率、AI 失败率、耗时和成本。
- 建立关键异常阈值和告警。

验收标准：

- 可以按日期、用户类型和推荐来源查看核心漏斗。
- 可以识别转化流失最大的步骤。
- AI 异常和成本超出阈值时能够及时发现。

### 7.2 OPT-011 家庭库存与保质期

优先级：P2  
涉及端：前端、后端、数据库、推荐服务

开发内容：

- 记录家庭现有食材、数量、单位和到期时间。
- 购物清单完成后可选择将食材加入库存。
- 做菜后自动或手动扣减库存。
- 推荐系统优先使用临期和已有食材。
- 提供临期提醒和清库存模式。

验收标准：

- 库存变化来源可追溯。
- 用户可以快速修正自动扣减结果。
- 临期食材能够真实影响推荐排序。

### 7.3 OPT-012 菜单模板与快捷复用

优先级：P2  
涉及端：前端、后端

开发内容：

- 保存家庭常用周菜单模板。
- 支持“重复上周菜单”并允许局部替换。
- 提供省钱、清库存、应季、快手等菜单模式。
- 根据用餐人数自动调整菜谱份量和购物数量。

验收标准：

- 老用户能够在一分钟内复用并调整一周菜单。
- 调整人数后菜谱和购物清单数量保持一致。
- 模板不会覆盖原始历史菜单。

### 7.4 OPT-013 情侣与家庭实时协作

优先级：P2  
涉及端：前端、后端、实时通信

开发内容：

- 双方共享同一购物清单，而不是分别复制。
- 支持实时勾选、添加和修改购物项。
- 显示操作人和最近更新时间。
- 对并发修改提供冲突处理。
- 将双方偏好和反馈纳入共同推荐。

验收标准：

- 两端修改能够在合理时间内同步。
- 重复购物项可以合并，并保留来源。
- 解绑后共享数据的归属和访问权限规则明确。

## 8. 数据埋点建议

建议统一事件结构：

| 字段 | 说明 |
| --- | --- |
| `event_name` | 事件名称 |
| `user_id` | 用户 ID |
| `session_id` | 会话 ID |
| `source_page` | 来源页面 |
| `recipe_id` | 菜谱 ID，可选 |
| `menu_id` | 菜单 ID，可选 |
| `recommend_type` | 推荐类型，可选 |
| `strategy_version` | 推荐策略版本，可选 |
| `result` | success、failed、cancelled |
| `duration_ms` | 操作或请求耗时 |
| `created_at` | 事件时间 |

建议优先补齐以下事件：

- `home_primary_action_clicked`
- `recommend_requested`
- `recommend_result_viewed`
- `recommend_result_rejected`
- `recipe_favorited`
- `recipe_added_to_menu`
- `shopping_list_generated`
- `shopping_item_completed`
- `recipe_marked_cooked`
- `recipe_feedback_submitted`
- `ai_generation_started`
- `ai_generation_completed`
- `ai_generation_failed`

## 9. 发布与验收清单

### 9.1 功能验收

- 首页所有可见按钮均有真实业务结果。
- 推荐到购物清单的核心流程完整可用。
- 收藏、反馈、复制和分享失败时有明确提示。
- 空状态提供下一步操作。
- 异步操作不可重复提交。

### 9.2 兼容性与体验

- 检查 375px、414px、768px、1024px、1440px 视口。
- 检查 Chrome、Edge 和移动端主流浏览器。
- 检查键盘导航、焦点状态和读屏标签。
- 检查弱网、超时、接口错误和空数据场景。
- 检查浅色、深色模式下的文字和边框对比度。

### 9.3 工程质量

- 前端 Lint、类型检查、单元测试和构建通过。
- 核心 Playwright 端到端测试通过。
- 后端 `go test ./...` 通过。
- 数据库迁移具有回滚或修复方案。
- 接口变更同步更新类型和接口文档。

### 9.4 生产安全

- 生产环境不存在默认密钥和开发数据库密码。
- Debug 和自动迁移已关闭。
- 登录、AI、上传和邀请码接口已限流。
- 日志不记录密码、Token 和敏感个人信息。
- AI 请求具备超时、配额和成本上限。

## 10. 风险与应对

| 风险 | 影响 | 应对方案 |
| --- | --- | --- |
| 营养数据不完整 | 统计结果误导用户 | 展示数据完整度，缺失值不按零计算 |
| 用户反馈数据较少 | 推荐优化效果不明显 | 采用轻量反馈，结合收藏、菜单和购物行为 |
| AI 调用成本上升 | 运营成本不可控 | 用户配额、缓存、并发限制和成本告警 |
| 自动化测试建设耗时 | 短期开发速度下降 | 优先覆盖核心流程，不追求一次性高覆盖率 |
| 实时协作复杂度高 | 数据冲突和重复项 | 先做共享版本与时间戳，再逐步引入实时通信 |
| 新旧埋点不一致 | 无法进行连续分析 | 建立事件字典和版本字段，保留迁移期映射 |

## 11. 优先级结论

当前阶段最值得投入的顺序是：

1. 修复无效交互和不完整状态，建立用户信任。
2. 建立自动化测试、CI 和生产安全底线。
3. 打通做饭反馈与推荐优化的数据闭环。
4. 使用真实营养数据重构统计页。
5. 建设运营漏斗、异常监控和 AI 成本看板。
6. 在核心流程稳定后，再投入库存、模板和实时家庭协作。

本阶段不建议继续增加大量独立页面。产品完成度的提升将主要来自核心链路的可靠性、推荐质量、反馈闭环和长期使用价值。
