# 今天吃什么迭代开发任务

版本：v1.0  
日期：2026-07-01  
来源文档：[产品迭代方案.md](./产品迭代方案.md)  
适用范围：`web-user` 用户端、`backend` 后端服务、数据库迁移、推荐算法、AI 生成、购物清单、情侣点餐

## 1. 迭代目标

本轮迭代的核心目标是将现有功能从“多个独立工具”收敛成一条完整的家庭用餐决策闭环：

```text
偏好 -> 推荐 -> 菜谱/菜单 -> 购物清单 -> 反馈 -> 更准推荐
```

开发侧重点：

- 降低用户找功能的成本。
- 前置偏好，让推荐更个性化。
- 增强 AI 生成过程的可控性和可信度。
- 打通菜谱、推荐、一周菜单到购物清单的闭环。
- 为后续推荐优化沉淀行为数据。

## 2. 任务优先级

| 优先级 | 含义 | 处理建议 |
| --- | --- | --- |
| P0 | 本轮必须完成 | 影响主流程闭环或关键体验 |
| P1 | 本轮建议完成 | 强化留存、反馈和差异化能力 |
| P2 | 可后续排期 | 菜单资产、画像、模板等增强能力 |

## 3. 开发阶段总览

| 阶段 | 主题 | 优先级 | 建议周期 |
| --- | --- | --- | --- |
| 阶段一 | 主流程收敛与信息架构调整 | P0 | 第 1 周 |
| 阶段二 | 偏好引导与 AI 可信体验 | P0 | 第 2 周 |
| 阶段三 | 购物清单闭环 | P0 | 第 3 周 |
| 阶段四 | 用户反馈与推荐优化 | P1 | 第 4 周 |
| 阶段五 | 情侣点餐融入主流程 | P1 | 第 4-5 周 |
| 阶段六 | 菜单资产沉淀 | P2 | 后续迭代 |

## 4. 阶段一：主流程收敛与信息架构调整

目标：让用户打开应用后能快速理解“今天怎么吃”，并在 30 秒内进入有效推荐流程。

### ITER-001 底部导航调整

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已将底部导航文案调整为：首页、推荐、菜谱、清单、我的。
- 已将“推荐”入口指向 `/recommend/scene`，保留原有 `/week-menu` 路由作为二级入口。
- 已增加推荐模块高亮规则，所有 `/recommend/...` 页面都会高亮“推荐”。

优先级：P0  
涉及端：前端  
涉及文件：

- `web-user/src/components/BottomTabBar.vue`
- `web-user/src/router/index.ts`

开发任务：

- 将底部导航“菜单推荐”改为“推荐”。
- 将底部导航“购物清单”改为“清单”。
- 评估是否将 `/week-menu` 从底部主导航中移除，改由推荐页入口进入。
- 若保留 `/week-menu` 路由，仍可作为推荐模块下的二级入口。

验收标准：

- 底部导航文案为：首页、推荐、菜谱、清单、我的。
- 点击“推荐”进入统一推荐页或推荐模块入口。
- 移动端底部文字不换行、不溢出。

测试建议：

- 前端构建：`npm.cmd run build`
- 手动检查 375px、414px、768px 宽度底部导航显示。

### ITER-002 首页主行动重组

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已将首页首屏主流程收敛为“快速推荐、用现有食材、安排本周”三个主行动。
- 已将原快捷功能降级为“按口味找菜、冰箱库存、换点新菜、最热菜谱”等次级入口。
- 今日推荐卡片已增加“查看做法、换一组、加入清单”动作。
- 情侣点餐入口已下沉为场景卡片，并改为“和 TA 一起决定”。

优先级：P0  
涉及端：前端、后端可选  
涉及文件：

- `web-user/src/views/Home.vue`
- `web-user/src/api/home.ts`
- `backend/internal/api/v1/home.go`
- `backend/internal/service/recommend_service.go`

开发任务：

- 首页主标题围绕“今天怎么吃”表达。
- 首页保留 3 个主行动入口：
  - 快速推荐
  - 用现有食材
  - 安排本周
- 将“按口味推荐、按场景推荐、冰箱剩菜、最热菜谱”等入口降级为次级入口或合并到推荐页。
- 首页展示最近购物清单摘要，用户可直接继续采购。
- 首页今日推荐卡片增加主按钮：查看做法、换一个、加入清单。

后端任务：

- 若首页需要展示最近购物清单，可新增或复用购物清单列表接口。
- 若首页需要快速推荐，可复用 `/api/recommend/menu`。

验收标准：

- 首页首屏只突出一个主题：“今天怎么吃”。
- 首屏至少有一个明确主 CTA。
- 用户从首页进入推荐流程不超过 1 次点击。

测试建议：

- 登录后访问首页，检查推荐入口是否可达。
- 后端暂无菜谱时，首页空状态不阻塞主流程。

### ITER-003 推荐页入口按用户意图重组

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已将推荐页入口重组为“我不知道吃什么、用现有食材做饭、按口味找菜、安排本周菜单、换点没吃过的新菜、我和 TA 一起决定”。
- 已保留旧的 `/recommend/:mode` 兼容方式，并新增 `new/week/couple` 模式映射。
- “安排本周菜单”和“我和 TA 一起决定”作为推荐页内跳转入口，不破坏现有 `/week-menu` 与 `/couple` 页面。

优先级：P0  
涉及端：前端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/api/recommend.ts`
- `web-user/src/types/recommend.ts`

开发任务：

- 将推荐入口文案调整为用户意图型：
  - 我不知道吃什么
  - 用现有食材做饭
  - 按口味找菜
  - 安排本周菜单
  - 换点没吃过的新菜
  - 我和 TA 一起决定
- 将原有 `ingredients/taste/scene/fridge` 模式映射到新入口。
- 推荐页顶部展示当前选择模式和下一步动作。
- 对不同入口提供对应默认参数：
  - 不知道吃什么：走场景推荐或默认菜单推荐。
  - 用现有食材做饭：走食材推荐。
  - 安排本周菜单：跳转或嵌入一周菜单。
  - 换点新菜：走 AI 生成。
  - 我和 TA 一起决定：跳转情侣点餐。

验收标准：

- 用户不需要理解“按场景/按食材”这样的技术型分类。
- 每个入口都有清晰说明和明确按钮。
- 推荐结果区域与入口选择保持一致。

测试建议：

- 每个入口至少手动走通一次。
- 检查路由刷新后当前模式是否保留。

### ITER-004 推荐结果动作统一

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 食材推荐结果已增加“查看做法、加入清单、缺少食材加入清单”等后续动作。
- 场景/AI 推荐结果已增加来源标识、整组加入清单、换一组和推荐理由展示。
- 当前版本优先复用现有购物清单能力，整组清单使用 `shopping_list` 字段合并；按多个 `recipe_id` 生成清单接口留待 `ITER-010` 完成。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/views/RecipeDetail.vue`
- `web-user/src/stores/shopping.ts`
- `web-user/src/api/shopping.ts`

开发任务：

- 推荐结果卡片统一展示：
  - 推荐理由
  - 查看做法
  - 加入购物清单
  - 换一组
- 对 AI 生成菜谱展示来源标识。
- 推荐菜单结果支持一键把所有菜品的缺少食材加入购物清单。

后端任务：

- 确认推荐结果返回 `recipe_id`、`reason`、`shopping_list`。
- 若缺少字段，补充响应结构。

验收标准：

- 推荐结果出现后，用户能继续执行下一步，而不是停在结果页。
- 推荐结果中每个菜都能进入菜谱详情。
- 缺少食材能被合并到当前购物清单或新建清单。

## 5. 阶段二：偏好引导与 AI 可信体验

目标：让推荐更准，并让用户理解 AI 正在做什么、生成了什么、是否可靠。

### ITER-005 新用户偏好引导

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `GET /api/user/preferences/status`，通过现有偏好字段推导完成度，不新增数据库字段。
- 登录/注册成功后会读取偏好状态，未完成时进入 `/user/preferences?guide=1` 轻量引导。
- 偏好页已覆盖 5 个问题：人数、口味、忌口、做饭时间、健康目标。
- 引导模式支持跳过，保存后自动回到首页；普通偏好设置仍保留原编辑流程。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Login.vue`
- `web-user/src/views/Preferences.vue`
- `web-user/src/router/index.ts`
- `web-user/src/stores/user.ts`
- `web-user/src/api/user.ts`
- `backend/internal/api/v1/user.go`
- `backend/internal/service/user_service.go`
- `backend/internal/repository/user_pref_repo.go`

开发任务：

- 登录后判断用户是否完成偏好设置。
- 未完成偏好时进入轻量偏好引导页或弹层。
- 引导问题控制在 5 个：
  - 人数
  - 口味
  - 忌口
  - 做饭时间
  - 健康目标
- 支持跳过，但跳过后在首页或推荐页保留“完善偏好”提示。

后端任务：

- 用户偏好增加完成状态判断，可通过已有字段推导，也可新增 `preference_completed`。
- `GET /api/user/preferences` 返回偏好完整度。

建议 API：

```http
GET /api/user/preferences
PUT /api/user/preferences
```

可选新增：

```http
GET /api/user/preferences/status
```

验收标准：

- 新用户首次登录后能看到偏好引导。
- 完成偏好后进入首页。
- 偏好保存后能影响推荐参数。

测试建议：

- 新账号首次登录。
- 已完成偏好的老账号登录。
- 跳过偏好后再次进入推荐页。

### ITER-006 推荐页偏好摘要与临时调整

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 推荐页顶部已展示本次推荐使用的人数、做饭时间、健康目标摘要。
- 已支持在推荐页临时调整人数、做饭时间、健康目标，临时参数只参与本次推荐请求。
- 已读取长期口味偏好和忌口，并在普通场景推荐、AI 场景推荐请求中合并传递。
- 长期偏好修改入口仍跳转 `/user/preferences`。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/views/Preferences.vue`
- `web-user/src/api/user.ts`
- `backend/internal/service/recommend_service.go`

开发任务：

- 推荐页顶部展示当前偏好摘要：
  - 人数
  - 口味
  - 健康目标
  - 忌口
  - 做饭时间
- 支持本次推荐临时修改偏好，不一定写回用户长期偏好。
- 长期偏好修改仍跳转偏好设置页。

后端任务：

- 推荐接口继续支持请求体中的临时参数覆盖用户默认偏好。
- AI 推荐继续读取用户偏好并合并临时参数。

验收标准：

- 用户能看到本次推荐使用了哪些偏好。
- 临时修改只影响本次推荐。
- 长期偏好更新后再次进入推荐页能同步展示。

### ITER-007 AI 生成过程状态

状态：部分完成  
完成时间：2026-07-01  
完成说明：

- 前端已增加 AI 生成过程状态：读取偏好、生成菜谱、校验食材、保存菜谱库。
- 已禁用生成期间重复点击，并保留成功/失败可读提示。
- 后端流式进度或异步任务状态尚未实现，当前采用前端本地阶段进度。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/api/recommend.ts`
- `backend/internal/service/ai_client.go`
- `backend/internal/service/ai_scene_service.go`
- `backend/internal/api/v1/recommend.go`

开发任务：

- AI 生成按钮点击后展示过程状态：
  - 读取偏好
  - 生成菜谱
  - 校验食材
  - 保存到菜谱库
- 生成期间禁用重复点击。
- 失败时展示可读错误和重试按钮。
- 成功时展示“已同步到菜谱库”。

后端任务：

- 当前同步接口可先不做流式进度。
- 前端可用本地阶段模拟进度，接口返回后切换成功/失败。
- 后续可考虑 SSE 或轮询任务状态。

验收标准：

- AI 生成时用户知道系统正在做什么。
- 超时、上游错误、格式错误都有清晰提示。
- 成功后生成的菜能进入详情页。

### ITER-008 AI 生成记录与排查字段

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `ai_generation_logs` 模型、仓储、服务与迁移 SQL。
- 已接入 AI 按菜名生成菜谱、AI 场景推荐、AI 生成购物清单三条路径。
- 成功和失败都会旁路写入日志，不影响主生成流程。
- 日志记录生成类型、用户 ID、场景、模型、输入摘要、输出摘要、状态、错误信息、耗时和菜谱 ID；不记录 API Key 等敏感字段。
- 已补充 AI 生成日志 JSON 与耗时基础单元测试，并通过 `go test ./...`。

优先级：P1  
涉及端：后端、数据库  
涉及文件：

- `backend/internal/model`
- `backend/internal/repository`
- `backend/internal/service/ai_client.go`
- `backend/internal/service/ai_scene_service.go`
- `backend/migrations`

开发任务：

- 新增 AI 生成日志表。
- 记录生成类型、用户 ID、输入参数、模型、状态、耗时、错误、生成菜谱 ID。
- AI 生成成功或失败均写日志。

建议数据表：`ai_generation_logs`

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| generation_type | varchar | scene_recipe / recipe / shopping |
| scene | varchar | 场景 |
| model | varchar | AI 模型 |
| input_json | json | 输入参数 |
| output_json | json | 输出摘要 |
| status | varchar | success / failed |
| error_message | text | 错误信息 |
| duration_ms | int | 耗时 |
| recipe_ids_json | json | 入库菜谱 ID |
| created_at | datetime | 创建时间 |

验收标准：

- 每次 AI 生成都有日志。
- 管理端或数据库可查失败原因。
- 不记录完整 API Key 等敏感信息。

## 6. 阶段三：购物清单闭环

目标：让推荐和菜谱可以自然转化为可执行采购清单。

### ITER-009 菜谱详情加入购物清单

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `POST /api/shopping-list/generate-by-recipe`，支持按 `recipe_id` 解析真实菜谱食材。
- 菜谱详情底部主按钮已改为“加入清单”，点击后合并到当前购物清单，无当前清单时自动新建。
- 首页今日推荐也优先使用 `recipe_id` 加入清单，保留按菜名生成作为兜底。
- 加入成功/失败均有中文提示。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/RecipeDetail.vue`
- `web-user/src/stores/shopping.ts`
- `web-user/src/api/shopping.ts`
- `backend/internal/api/v1/shopping.go`
- `backend/internal/service/shopping_service.go`

开发任务：

- 菜谱详情底部增加“加入清单”主按钮。
- 点击后将当前菜谱食材加入当前购物清单。
- 若无当前清单，则新建清单。
- 已加入的食材提示“已合并到购物清单”。

后端任务：

- 可复用现有 `generate-by-dish`。
- 建议新增按 `recipe_id` 生成清单接口，避免按菜名模糊匹配。

建议 API：

```http
POST /api/shopping-list/generate-by-recipe
```

请求：

```json
{
  "recipe_id": 1,
  "merge_current": true
}
```

验收标准：

- 从菜谱详情到加入购物清单不超过 2 次点击。
- 食材能正确进入购物清单。
- 重复加入时能合并或提示已存在。

### ITER-010 推荐菜单生成购物清单

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `POST /api/shopping-list/generate-by-recipes`，支持多个 `recipe_id` 合并生成采购项。
- 推荐页场景/AI 结果的“整组加入清单”已优先使用多个 `recipe_id` 合并食材。
- 食材/冰箱推荐结果中的单菜加入清单已改为优先按 `recipe_id` 生成。
- 缺少食材场景仍支持只把缺少项合并到当前购物清单。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/api/shopping.ts`
- `backend/internal/api/v1/shopping.go`
- `backend/internal/service/shopping_service.go`

开发任务：

- 推荐结果增加“一键生成采购清单”。
- 对推荐菜单中的多个菜谱合并食材。
- 缺少食材场景下可只加入缺少项。

后端任务：

- 新增按多个 recipe_id 生成购物清单接口。
- 支持 `merge_current`。
- 支持 `only_missing` 可选参数。

建议 API：

```http
POST /api/shopping-list/generate-by-recipes
```

请求：

```json
{
  "recipe_ids": [1, 2, 3],
  "name": "今日推荐采购清单",
  "merge_current": true,
  "only_missing": false
}
```

验收标准：

- 推荐结果中的多道菜能生成一个清单。
- 相同食材不会重复出现多条。
- 生成后可跳转购物清单页查看。

### ITER-011 一周菜单整周采购清单

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 一周菜单页已增加“生成整周采购清单”按钮。
- 整周清单优先汇总周菜单里的 `recipe_id` 并调用多菜谱清单接口。
- 若周菜单缺少 `recipe_id`，会退回按周菜单食材列表合并到当前购物清单。
- 原有按天加入采购清单能力保留。

优先级：P0  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/WeekMenu.vue`
- `web-user/src/api/shopping.ts`
- `backend/internal/api/v1/shopping.go`
- `backend/internal/service/shopping_service.go`

开发任务：

- 一周菜单页增加“生成整周采购清单”。
- 支持按天生成和整周合并生成。
- 清单名称默认：`本周菜单采购清单`。

后端任务：

- 若周菜单结果未持久化，可前端传 recipe_ids 或食材列表。
- 若后续保存周菜单，则可按 menu_id 生成。

验收标准：

- 周菜单中所有菜品可合并成一个采购清单。
- 用户能从周菜单页跳转到购物清单。

### ITER-012 购物清单合并重复食材

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 后端已增加食材名称规范化与合并逻辑，支持 `番茄/西红柿`、`蛋/鸡蛋`、`马铃薯/土豆` 等别名。
- 同名同单位数量可合并，例如 `2个 + 3个 = 5个`；不同单位会保留原始说明。
- 前端购物清单分类已调整为蔬菜、肉蛋水产、主食、调味、配料、其他。
- 已补充购物清单合并单元测试。

优先级：P0  
涉及端：后端、前端  
涉及文件：

- `backend/internal/service/shopping_service.go`
- `backend/internal/service/shopping_service_test.go`
- `web-user/src/views/ShoppingList.vue`

开发任务：

- 合并同名或别名食材。
- 数量可合并时合并，不可合并时保留备注。
- 分类统一为蔬菜、肉蛋水产、主食、调味、配料、其他。
- 前端按类别分组展示。

后端任务：

- 增加食材名称规范化逻辑。
- 增加合并函数单元测试。

验收标准：

- 多个菜谱都包含鸡蛋时，清单只显示一项鸡蛋。
- 无法精确合并单位时，不丢失原始信息。
- 清单按类别展示。

测试建议：

- 单位相同：`2个 + 3个 = 5个`
- 单位不同：`1勺 + 适量` 保留说明
- 别名：`番茄` 与 `西红柿`

### ITER-013 家里已有食材状态

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 购物清单食材已支持 `pending/bought/owned` 三种状态，并兼容旧数据中的 `checked` 字段。
- 购物清单页已增加“待买、已买、已有”三态切换，复制/分享清单会带上食材状态。
- 预估总价和待采购逻辑只统计 `pending` 食材，`bought/owned` 不再计入待采购。
- 菜谱详情食材区可标记“家里已有”，加入清单时会把对应食材合并为 `owned` 状态。
- 后端生成清单默认写入 `pending` 状态，并补充了已有食材合并单元测试。
- 已通过 `go test ./...` 与 `npm.cmd run build`。

优先级：P1  
涉及端：前端、后端、数据库  
涉及文件：

- `web-user/src/views/ShoppingList.vue`
- `web-user/src/views/RecipeDetail.vue`
- `backend/internal/model/shopping_list.go`
- `backend/internal/api/v1/shopping.go`

开发任务：

- 购物清单食材增加状态：
  - 待购买
  - 已购买
  - 家里已有
- 菜谱详情食材区支持标记家里已有。
- 推荐时可优先使用家里已有食材。

数据建议：

- 当前 `items_json` 中增加字段 `owned: boolean` 或 `status: pending/bought/owned`。

验收标准：

- 用户可将食材标记为家里已有。
- 家里已有食材不会被算作待采购。

## 7. 阶段四：用户反馈与推荐优化

目标：沉淀用户行为，让推荐越用越准。

### ITER-014 菜谱反馈数据模型

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `user_recipe_feedbacks` 模型、仓储、服务与迁移 SQL。
- 已支持 `cooked/like/dislike/block` 四类反馈，并通过唯一索引保证同一反馈幂等。
- 已新增 `POST /api/recipes/:id/feedback`、`DELETE /api/recipes/:id/feedback/:type`、`GET /api/user/recipe-feedback`。
- 菜谱详情接口已返回当前用户对该菜谱的反馈状态。

优先级：P1  
涉及端：后端、数据库  
涉及文件：

- `backend/internal/model`
- `backend/internal/repository`
- `backend/internal/service`
- `backend/internal/api/v1`
- `backend/migrations`

开发任务：

- 新增用户菜谱反馈表。
- 支持反馈类型：
  - cooked 做过了
  - like 喜欢
  - dislike 不喜欢
  - block 不想再推荐
- 同一用户对同一菜谱的同一反馈可更新或幂等。

建议数据表：`user_recipe_feedbacks`

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| recipe_id | bigint | 菜谱 ID |
| feedback_type | varchar | cooked / like / dislike / block |
| source | varchar | detail / recommend / couple |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

建议 API：

```http
POST /api/recipes/:id/feedback
DELETE /api/recipes/:id/feedback/:type
GET /api/user/recipe-feedback
```

验收标准：

- 用户可以对菜谱提交反馈。
- 重复提交不会产生异常重复数据。
- 后端能查询用户对某菜谱的反馈状态。

### ITER-015 菜谱详情反馈入口

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 菜谱详情页已增加“做过了、喜欢、不喜欢、不再推荐”反馈入口。
- 反馈提交或取消后会即时更新按钮状态并展示可读提示。
- “不再推荐”提交前已增加二次确认，降低误触风险。
- 刷新菜谱详情页后会从后端同步已保存的反馈状态。

优先级：P1  
涉及端：前端  
涉及文件：

- `web-user/src/views/RecipeDetail.vue`
- `web-user/src/api/recipe.ts`
- `web-user/src/types/recipe.ts`

开发任务：

- 菜谱详情页增加反馈动作：
  - 做过了
  - 喜欢
  - 不喜欢
  - 不想再推荐
- 反馈后展示即时状态。
- 不喜欢/不想再推荐需要二次确认或轻提示，避免误触。

验收标准：

- 用户反馈成功后状态立即更新。
- 刷新页面后反馈状态仍保留。

### ITER-016 推荐算法接入反馈权重

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 普通场景推荐已读取用户菜谱反馈，`block` 菜谱直接过滤。
- `dislike` 菜谱降低推荐权重，`cooked` 菜谱降低短期重复推荐概率。
- `like` 菜谱会提高同口味、同健康标签菜谱的推荐权重。
- 食材/冰箱推荐也已接入 `block/dislike/cooked/like` 的过滤和排序调整。

优先级：P1  
涉及端：后端  
涉及文件：

- `backend/internal/service/recommend_service.go`
- `backend/internal/repository`

开发任务：

- 推荐排序降低 dislike 菜谱权重。
- block 菜谱不再推荐给该用户。
- cooked 菜谱可降低短期重复推荐概率。
- like 菜谱的相似口味/分类可增加权重。

验收标准：

- 用户标记“不想再推荐”的菜不会再次出现在推荐结果中。
- 用户喜欢的口味在推荐中有更高出现概率。

### ITER-017 行为事件记录

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已新增 `user_events` 模型、仓储、服务、接口与迁移 SQL。
- 已新增 `POST /api/user/events`，用于记录用户关键行为。
- 前端已封装 `trackEvent`，埋点失败不会影响主流程。
- 已接入首页主行动点击、推荐发起、推荐结果点击、加入购物清单、收藏、菜谱反馈、周菜单生成清单等关键路径。

优先级：P1  
涉及端：前端、后端、数据库  
涉及文件：

- `backend/internal/model`
- `backend/internal/api/v1`
- `web-user/src/api`

开发任务：

- 记录关键行为：
  - 首页主行动点击
  - 推荐发起
  - 推荐结果曝光
  - 推荐结果点击
  - 加入购物清单
  - 收藏
  - 反馈
- 前端封装统一埋点函数。

建议数据表：`user_events`

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| event_name | varchar | 事件名 |
| entity_type | varchar | recipe / menu / shopping_list |
| entity_id | bigint | 关联 ID |
| payload_json | json | 扩展信息 |
| created_at | datetime | 创建时间 |

验收标准：

- 关键路径事件可在数据库查询。
- 前端埋点失败不影响主流程。

## 8. 阶段五：情侣点餐融入主流程

目标：让情侣点餐成为自然使用场景，而不是孤立页面。

### ITER-018 菜谱详情发给 TA

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 菜谱详情底部已增加“发给 TA 想吃”图标按钮。
- 已复用 `POST /api/couple/orders` 创建情侣点餐记录，默认餐次为晚餐、日期为当天。
- 未绑定情侣关系时会展示可读错误，并引导进入情侣绑定页。
- 已接入行为事件 `couple_order_create`。

优先级：P1  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/RecipeDetail.vue`
- `web-user/src/api/couple.ts`
- `backend/internal/api/v1/couple.go`
- `backend/internal/service/couple_service.go`

开发任务：

- 菜谱详情增加“发给 TA 想吃”按钮。
- 绑定情侣后可直接创建情侣点餐记录。
- 未绑定时引导进入情侣绑定页。

后端任务：

- 可复用情侣订单创建接口。
- 默认 meal_date 为当天，meal_type 可让用户选择或默认 dinner。

验收标准：

- 已绑定用户能从菜谱详情创建情侣点餐。
- 未绑定用户点击后进入绑定引导。

### ITER-019 推荐结果加入情侣候选

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 食材/冰箱推荐结果卡片已增加“和 TA 吃”动作。
- 场景/AI 推荐结果中的单道菜已增加“和 TA 吃”按钮。
- 点击后复用情侣点餐订单接口，将推荐菜转为情侣候选。
- 未绑定情侣关系时会引导进入情侣绑定页。

优先级：P1  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/Recommend.vue`
- `web-user/src/api/couple.ts`
- `backend/internal/api/v1/couple.go`

开发任务：

- 推荐结果卡片增加“和 TA 吃”。
- 点击后将菜加入情侣点餐候选。
- 支持选择日期和餐次。

验收标准：

- 推荐结果可以直接转为情侣点餐。
- 成功后情侣订单列表可看到该菜。

### ITER-020 合意菜单折中推荐

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 情侣点餐生成接口已返回 `agreed_dishes` 与 `compromise_dishes`。
- 双方点餐有相同菜谱或相同菜名时，会优先展示“双方都想吃”的合意菜。
- 无重合时，会根据双方偏好、候选菜、口味、健康标签、烹饪时长和热度生成折中推荐。
- 折中推荐已展示可读原因，例如兼顾双方口味、照顾健康目标、做起来不费时间。
- 合意菜单页已展示合意/折中推荐，并可进入菜谱做法。

优先级：P1  
涉及端：后端、前端  
涉及文件：

- `backend/internal/service/couple_service.go`
- `backend/internal/service/recommend_service.go`
- `web-user/src/views/CoupleMenu.vue`

开发任务：

- 合意菜单优先展示双方都想吃的菜。
- 若没有重合，根据双方偏好生成折中推荐。
- 折中推荐展示原因，例如“兼顾清淡和高蛋白”。

验收标准：

- 双方选择有重合时优先展示重合菜。
- 无重合时系统能给出折中推荐。

### ITER-021 情侣菜单生成共享购物清单

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 合意菜单页已将主按钮调整为“生成共享购物清单”。
- 情侣点餐采购接口已支持 `save_shared` 参数，开启后会为双方各自生成一份同名采购清单。
- 清单名称默认包含日期与“情侣点餐采购清单”，食材来自合意菜优先，其次来自折中推荐和已确认点餐菜谱。
- 生成后双方都能在各自购物清单页查看，当前版本采用“复制到双方清单”的共享落地方式。
- 已通过 `go test ./...` 与 `npm.cmd run build`。

优先级：P1  
涉及端：前端、后端  
涉及文件：

- `web-user/src/views/CoupleMenu.vue`
- `web-user/src/api/shopping.ts`
- `backend/internal/api/v1/couple.go`
- `backend/internal/api/v1/shopping.go`

开发任务：

- 合意菜单页增加“生成共享购物清单”。
- 清单名称默认：`情侣点餐采购清单`。
- 支持双方查看同一清单或复制到各自清单。

验收标准：

- 合意菜单可一键生成购物清单。
- 清单食材来自合意菜单菜谱。

## 9. 阶段六：菜单资产沉淀

目标：让用户形成自己的家庭菜单库。

### ITER-022 我的菜单模型与接口

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 已复用现有 `menus` 表承载用户菜单资产，不额外新增 `user_menus` 表，避免重复模型。
- 已新增菜单服务与用户侧接口：查询、保存、详情、删除、复用。
- 已支持保存推荐菜单、一周菜单、情侣菜单/AI 菜单等类型快照。
- 复用菜单会解析菜单快照中的 `recipe_id`，返回可用于生成购物清单的菜谱 ID。
- 已新增菜单索引迁移 `008_menus_indexes.sql`。

优先级：P2  
涉及端：后端、数据库  
涉及文件：

- `backend/internal/model`
- `backend/internal/repository`
- `backend/internal/api/v1`
- `backend/internal/service`
- `backend/migrations`

开发任务：

- 新增用户菜单表。
- 支持保存推荐菜单、一周菜单、情侣菜单。
- 支持查询、删除、复用。

建议数据表：`user_menus`

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| name | varchar | 菜单名 |
| menu_type | varchar | daily / weekly / couple / ai |
| recipe_ids_json | json | 菜谱 ID |
| menu_json | json | 完整菜单快照 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

建议 API：

```http
GET /api/user/menus
POST /api/user/menus
GET /api/user/menus/:id
DELETE /api/user/menus/:id
POST /api/user/menus/:id/reuse
```

验收标准：

- 用户可保存当前推荐菜单。
- 用户可查看历史保存菜单。
- 用户可从历史菜单生成购物清单。

### ITER-023 我的菜单前端页面

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 用户中心“我的菜单”入口已指向 `/user/menus`。
- 已新增 `MyMenus.vue`，展示已保存菜单、菜品摘要、保存时间与菜单类型。
- 我的菜单页支持删除菜单、复用菜单并生成购物清单。
- 推荐页场景/AI 推荐结果已增加“保存菜单”动作。
- 一周菜单页已增加“保存本周菜单”动作。

优先级：P2  
涉及端：前端  
涉及文件：

- `web-user/src/router/index.ts`
- `web-user/src/views/UserCenter.vue`
- 新增 `web-user/src/views/MyMenus.vue`
- `web-user/src/api/menu.ts`

开发任务：

- 用户中心新增“我的菜单”入口。
- 展示已保存菜单列表。
- 菜单详情展示菜谱组合。
- 支持复用菜单和生成购物清单。

验收标准：

- 用户中心可进入我的菜单。
- 菜单可复用到推荐或购物清单。

### ITER-024 常用菜单模板

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 一周菜单页已增加常用模板选择：工作日快手餐、周末聚餐、减脂周菜单、儿童营养餐。
- 模板会影响周菜单生成参数，包括人数、健康目标与做饭时长偏好。
- 保存周菜单时会记录使用的模板信息，便于后续复用和分析。
- 当前版本先用前端模板预设落地，后续可扩展为服务端可配置模板。

优先级：P2  
涉及端：前端、后端  

开发任务：

- 支持创建模板：
  - 工作日快手餐
  - 周末聚餐
  - 减脂周菜单
  - 儿童营养餐
- 生成周菜单时可选择模板。

验收标准：

- 用户能基于模板生成菜单。
- 模板能保留偏好和推荐参数。

## 10. 跨阶段基础任务

### ITER-025 API 错误提示统一

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 前端 axios 响应拦截器已统一转换常见错误文案。
- 已覆盖超时、网络异常、登录过期、AI 未配置、AI 上游不可用、返回格式异常等场景。
- 后端新增接口错误文案均使用中文可读提示。

优先级：P0  
涉及端：前端、后端  

开发任务：

- 前端 axios 错误提示统一处理。
- 后端错误码与错误消息保持一致。
- AI 相关错误区分：
  - 未配置
  - 上游不可用
  - 超时
  - 返回格式异常
  - 保存失败

验收标准：

- 用户端不出现英文技术错误。
- 关键失败场景有明确重试或下一步建议。

### ITER-026 推荐结果数据结构统一

状态：已完成  
完成时间：2026-07-01  
完成说明：

- 推荐页已按统一字段消费 `menu_name/reason/source/dishes/shopping_list`。
- 普通场景推荐和 AI 场景推荐均展示来源、推荐理由、菜品列表和购物清单动作。
- 周菜单接口仍使用原周菜单结构，待后续 `ITER-011` 继续统一整周采购流程。

优先级：P0  
涉及端：前端、后端  

开发任务：

- 统一普通推荐、AI 推荐、周菜单推荐的结果字段。
- 前端减少不同推荐类型的特殊判断。

建议统一结构：

```json
{
  "menu_name": "推荐菜单",
  "reason": "推荐理由",
  "source": "database | ai | couple | weekly",
  "dishes": [
    {
      "recipe_id": 1,
      "name": "番茄炒蛋",
      "type": "主菜",
      "reason": "符合清淡口味",
      "cook_time": 15,
      "difficulty": "简单",
      "ingredients": ["番茄", "鸡蛋"]
    }
  ],
  "shopping_list": ["番茄", "鸡蛋"]
}
```

验收标准：

- 推荐页可以使用同一套结果组件渲染不同来源推荐。
- AI 推荐和普通推荐都有来源与推荐理由。

### ITER-027 测试补充

状态：部分完成  
完成时间：2026-07-01  
完成说明：

- 本轮已完成第一批 P0 前端主流程验证。
- 已执行 `npm.cmd run build`，前端构建通过。
- 已执行 `go test ./...`，后端测试通过。
- 已补充购物清单食材合并单元测试，覆盖别名合并、同单位数量相加、不同单位保留说明。
- 已补充菜谱反馈状态映射与反馈权重单元测试。
- 已补充行为事件 JSON 记录基础测试。
- 第二批/第三批 P0 完成后已再次执行 `npm.cmd run build` 与 `go test ./...`，均通过。
- 已补充 AI 生成日志基础测试、购物清单状态合并测试。
- 情侣合意菜单、共享购物清单、我的菜单与模板功能完成后已再次执行 `npm.cmd run build` 与 `go test ./...`，均通过。
- 仍建议后续补充浏览器端 E2E 和真实设备手动验收。

优先级：P0  
涉及端：前端、后端  

后端测试：

- 推荐参数合并逻辑。
- AI 返回解析兼容。
- 购物清单食材合并。
- 用户反馈幂等。
- 情侣菜单合意逻辑。

前端测试或手动用例：

- 新用户偏好引导。
- 推荐入口全链路。
- AI 生成成功/失败。
- 菜谱加入购物清单。
- 周菜单生成购物清单。
- 情侣点餐到购物清单。

验收标准：

- 后端 `go test ./...` 通过。
- 前端 `npm.cmd run build` 通过。
- P0 主路径手动验收通过。

## 11. 建议开发顺序

第一批 P0：

1. ITER-001 底部导航调整
2. ITER-002 首页主行动重组
3. ITER-003 推荐页入口按用户意图重组
4. ITER-004 推荐结果动作统一
5. ITER-026 推荐结果数据结构统一

第二批 P0：

1. ITER-005 新用户偏好引导
2. ITER-006 推荐页偏好摘要与临时调整
3. ITER-007 AI 生成过程状态
4. ITER-025 API 错误提示统一

第三批 P0：

1. ITER-009 菜谱详情加入购物清单
2. ITER-010 推荐菜单生成购物清单
3. ITER-011 一周菜单整周采购清单
4. ITER-012 购物清单合并重复食材

第四批 P1：

1. ITER-014 菜谱反馈数据模型
2. ITER-015 菜谱详情反馈入口
3. ITER-016 推荐算法接入反馈权重
4. ITER-017 行为事件记录
5. ITER-018 菜谱详情发给 TA
6. ITER-021 情侣菜单生成共享购物清单

## 12. 数据库迁移清单

本轮可能新增或调整的数据表：

- `ai_generation_logs`
- `user_recipe_feedbacks`
- `user_events`
- `user_menus`

可能调整的 JSON 字段：

- `shopping_lists.items_json`
  - 增加 `status` 或 `owned`
  - 增加合并后的来源信息
- `recipes`
  - 可选增加 `source`
  - 可选增加 `verified`
  - 可选增加 `dislike_count`
  - 可选增加 `cooked_count`

迁移要求：

- 不破坏现有数据。
- JSON 字段兼容旧格式。
- 所有新增表需要索引 `user_id`、`created_at`。

## 13. 接口清单

优先新增接口：

```http
POST /api/shopping-list/generate-by-recipe
POST /api/shopping-list/generate-by-recipes
POST /api/recipes/:id/feedback
DELETE /api/recipes/:id/feedback/:type
POST /api/user/events
```

可选新增接口：

```http
GET /api/user/preferences/status
GET /api/user/menus
POST /api/user/menus
GET /api/user/menus/:id
DELETE /api/user/menus/:id
POST /api/user/menus/:id/reuse
```

可复用接口：

```http
GET /api/user/preferences
PUT /api/user/preferences
POST /api/recommend/menu
POST /api/recommend/menu-ai
POST /api/recommend/by-ingredients
POST /api/recommend/week-menu
POST /api/couple/orders
POST /api/couple/orders/generate-shopping-list
```

## 14. 验收总标准

P0 版本完成后应满足：

- 用户首次登录能完成偏好引导或跳过。
- 首页能明确引导用户开始一次用餐决策。
- 推荐页入口按用户意图组织。
- 普通推荐和 AI 推荐都能展示结果、理由和下一步动作。
- 菜谱详情可以加入购物清单。
- 推荐菜单和周菜单可以生成购物清单。
- 购物清单能合并重复食材。
- 前端构建通过。
- 后端测试通过。

建议验收命令：

```powershell
cd backend
go test ./...
```

```powershell
cd web-user
npm.cmd run build
```

## 15. 风险点

### 15.1 推荐入口调整影响现有路由

应对：

- 保留旧路由兼容。
- 推荐页内部做 mode 映射。
- 不直接删除 `/recommend/:mode`。

### 15.2 购物清单合并规则复杂

应对：

- 第一版只做同名同单位合并。
- 不同单位先保留原始备注。
- 后续再做单位换算。

### 15.3 AI 生成耗时和失败

应对：

- 前端展示过程状态。
- 后端记录 AI 生成日志。
- 保留从菜谱库推荐作为稳定兜底。

### 15.4 偏好引导打断用户

应对：

- 支持跳过。
- 控制在 5 个问题以内。
- 跳过后在首页轻提示，不强制阻塞。

## 16. Definition of Done

每个开发任务完成时必须满足：

- 功能主路径可手动走通。
- 错误状态有可读提示。
- 移动端 375px 宽度无明显布局溢出。
- 不破坏已有登录、菜谱浏览、收藏、购物清单基础功能。
- 后端改动有必要的单元测试。
- 前端改动至少通过构建验证。
- 涉及数据库变更时提供迁移 SQL。
