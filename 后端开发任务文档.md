# 后端开发任务文档

## 项目概述

本项目后端采用 Python FastAPI 框架，提供 RESTful API 服务，支持用户端 Web App 和管理后台。

---

## 一、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | ^3.11 | 编程语言 |
| FastAPI | ^0.109 | Web 框架 |
| SQLAlchemy | ^2.0 | ORM |
| PyMySQL | ^1.1 | MySQL 驱动 |
| Redis | ^5.0 | 缓存 |
| python-jose | ^3.3 | JWT 处理 |
| passlib | ^1.7 | 密码加密 |
| python-multipart | ^0.0.6 | 文件上传 |
| uvicorn | ^0.27 | ASGI 服务器 |
| alembic | ^1.13 | 数据库迁移 |
| pytest | ^8.0 | 测试框架 |

---

## 二、项目结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 应用入口
│   ├── config.py               # 配置文件
│   ├── database.py             # 数据库连接
│   ├── redis.py                # Redis 连接
│   ├── dependencies.py         # 依赖注入
│   ├── api/                    # API 路由
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── router.py       # 路由汇总
│   │   │   ├── auth.py         # 认证接口
│   │   │   ├── user.py         # 用户接口
│   │   │   ├── home.py         # 首页接口
│   │   │   ├── recipe.py       # 菜谱接口
│   │   │   ├── category.py     # 分类接口
│   │   │   ├── recommend.py    # 推荐接口
│   │   │   ├── shopping.py     # 购物清单接口
│   │   │   ├── menu.py         # 菜单接口
│   │   │   ├── favorite.py     # 收藏接口
│   │   │   ├── feedback.py     # 反馈接口
│   │   │   └── banner.py       # 轮播图接口
│   │   └── admin/
│   │       ├── __init__.py
│   │       ├── router.py       # 后台路由汇总
│   │       ├── auth.py         # 后台认证
│   │       ├── dashboard.py    # 数据概览
│   │       ├── recipe.py       # 菜谱管理
│   │       ├── category.py     # 分类管理
│   │       ├── ingredient.py   # 食材管理
│   │       ├── banner.py       # 轮播图管理
│   │       ├── user.py         # 用户管理
│   │       ├── recommend_log.py # 推荐记录
│   │       └── feedback.py     # 反馈管理
│   ├── models/                 # 数据库模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── user_preference.py
│   │   ├── recipe.py
│   │   ├── recipe_category.py
│   │   ├── ingredient.py
│   │   ├── favorite.py
│   │   ├── menu.py
│   │   ├── shopping_list.py
│   │   ├── recommend_log.py
│   │   ├── feedback.py
│   │   ├── banner.py
│   │   └── admin_user.py
│   ├── schemas/                # Pydantic 模型
│   │   ├── __init__.py
│   │   ├── base.py             # 基础响应模型
│   │   ├── auth.py
│   │   ├── user.py
│   │   ├── recipe.py
│   │   ├── category.py
│   │   ├── ingredient.py
│   │   ├── recommend.py
│   │   ├── shopping.py
│   │   ├── menu.py
│   │   ├── favorite.py
│   │   ├── feedback.py
│   │   ├── banner.py
│   │   └── admin.py
│   ├── services/               # 业务逻辑
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── user_service.py
│   │   ├── recipe_service.py
│   │   ├── category_service.py
│   │   ├── ingredient_service.py
│   │   ├── recommend_service.py  # 推荐算法
│   │   ├── ai_service.py        # AI 推荐（预留）
│   │   ├── shopping_service.py
│   │   ├── menu_service.py
│   │   ├── favorite_service.py
│   │   ├── feedback_service.py
│   │   └── banner_service.py
│   ├── utils/                  # 工具函数
│   │   ├── __init__.py
│   │   ├── security.py         # JWT 和密码加密
│   │   ├── response.py         # 统一响应
│   │   ├── cache.py            # 缓存工具
│   │   └── validators.py       # 验证工具
│   └── exceptions.py           # 自定义异常
├── tests/                      # 测试
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_auth.py
│   ├── test_recipe.py
│   ├── test_recommend.py
│   └── test_shopping.py
├── alembic/                    # 数据库迁移
│   ├── versions/
│   ├── env.py
│   └── script.py.mako
├── alembic.ini
├── requirements.txt
├── .env.example
└── README.md
```

---

## 三、数据库设计

### 3.1 ER 图

```
┌─────────────┐       ┌─────────────────┐
│   users     │       │ user_preferences│
├─────────────┤       ├─────────────────┤
│ id (PK)     │──1:1──│ id (PK)         │
│ username    │       │ user_id (FK)    │
│ email       │       │ taste_preference│
│ phone       │       │ health_goal     │
│ password_hash│      │ avoid_ingredients│
│ nickname    │       │ ...             │
│ avatar      │       └─────────────────┘
│ ...         │
└─────────────┘
       │
       │ 1:N
       ▼
┌─────────────┐       ┌─────────────────┐
│  favorites  │       │ shopping_lists  │
├─────────────┤       ├─────────────────┤
│ id (PK)     │       │ id (PK)         │
│ user_id (FK)│       │ user_id (FK)    │
│ recipe_id   │       │ name            │
│ created_at  │       │ items_json      │
└─────────────┘       │ ...             │
       │              └─────────────────┘
       │
       ▼
┌─────────────┐       ┌─────────────────┐
│  recipes    │       │recipe_categories│
├─────────────┤       ├─────────────────┤
│ id (PK)     │──N:1──│ id (PK)         │
│ title       │       │ name            │
│ category_id │       │ icon            │
│ cover       │       │ sort            │
│ ...         │       │ status          │
└─────────────┘       └─────────────────┘
```

### 3.2 表结构定义

#### users 用户表

```sql
CREATE TABLE `users` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) UNIQUE NOT NULL,
  `email` VARCHAR(100) UNIQUE,
  `phone` VARCHAR(20) UNIQUE,
  `password_hash` VARCHAR(255) NOT NULL,
  `nickname` VARCHAR(50),
  `avatar` VARCHAR(500),
  `gender` TINYINT DEFAULT 0 COMMENT '0-未知 1-男 2-女',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_username` (`username`),
  INDEX `idx_email` (`email`),
  INDEX `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### user_preferences 用户偏好表

```sql
CREATE TABLE `user_preferences` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `taste_preference` JSON COMMENT '口味偏好：["咸鲜","酸甜","麻辣"]',
  `health_goal` VARCHAR(50) COMMENT '饮食目标：普通/减脂/增肌/控糖/儿童营养',
  `avoid_ingredients` JSON COMMENT '忌口食材：["香菜","芹菜"]',
  `favorite_ingredients` JSON COMMENT '喜欢的食材：["牛肉","虾"]',
  `cook_time_preference` VARCHAR(50) COMMENT '烹饪时间偏好：quick/medium/long',
  `people_count` INT DEFAULT 2 COMMENT '常用用餐人数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  UNIQUE INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### recipes 菜谱表

```sql
CREATE TABLE `recipes` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `title` VARCHAR(100) NOT NULL,
  `cover` VARCHAR(500),
  `description` TEXT,
  `category_id` BIGINT,
  `cook_time` INT COMMENT '烹饪时间（分钟）',
  `difficulty` VARCHAR(20) COMMENT '简单/中等/困难',
  `people_count` INT COMMENT '适合人数',
  `taste` VARCHAR(50) COMMENT '口味：咸鲜/酸甜/麻辣/清淡',
  `health_tags` JSON COMMENT '健康标签：["低脂","高蛋白","素食"]',
  `ingredients` JSON COMMENT '食材清单：[{"name":"鸡蛋","amount":"2个"}]',
  `seasonings` JSON COMMENT '调料清单：[{"name":"盐","amount":"适量"}]',
  `steps` JSON COMMENT '步骤：[{"step":1,"desc":"...","image":"..."}]',
  `tips` TEXT COMMENT '小技巧',
  `nutrition` JSON COMMENT '营养信息：{"calories":100,"protein":10,...}',
  `view_count` INT DEFAULT 0,
  `favorite_count` INT DEFAULT 0,
  `status` TINYINT DEFAULT 1 COMMENT '0-下架 1-上架',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`category_id`) REFERENCES `recipe_categories`(`id`),
  INDEX `idx_category` (`category_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_title` (`title`),
  FULLTEXT INDEX `ft_title_desc` (`title`, `description`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### recipe_categories 菜谱分类表

```sql
CREATE TABLE `recipe_categories` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL,
  `icon` VARCHAR(500),
  `sort` INT DEFAULT 0,
  `status` TINYINT DEFAULT 1 COMMENT '0-禁用 1-启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX `idx_sort` (`sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### ingredients 食材表

```sql
CREATE TABLE `ingredients` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL,
  `category` VARCHAR(50) COMMENT '食材分类：蔬菜/肉类/海鲜/调料',
  `image` VARCHAR(500),
  `nutrition_info` JSON COMMENT '营养信息',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX `idx_name` (`name`),
  INDEX `idx_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### favorites 收藏表

```sql
CREATE TABLE `favorites` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `recipe_id` BIGINT NOT NULL,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`recipe_id`) REFERENCES `recipes`(`id`) ON DELETE CASCADE,
  UNIQUE INDEX `idx_user_recipe` (`user_id`, `recipe_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### menus 菜单表

```sql
CREATE TABLE `menus` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `menu_name` VARCHAR(100),
  `meal_type` VARCHAR(20) COMMENT 'breakfast/lunch/dinner',
  `people_count` INT,
  `taste` VARCHAR(50),
  `health_goal` VARCHAR(50),
  `dishes_json` JSON COMMENT '菜品列表',
  `shopping_list_json` JSON COMMENT '购物清单',
  `reason` TEXT COMMENT '推荐理由',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### shopping_lists 购物清单表

```sql
CREATE TABLE `shopping_lists` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `name` VARCHAR(100),
  `items_json` JSON COMMENT '清单项：[{"name":"鸡蛋","amount":"2个","category":"蛋类","checked":false}]',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### recommend_logs 推荐记录表

```sql
CREATE TABLE `recommend_logs` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT,
  `recommend_type` VARCHAR(50) COMMENT 'menu/ingredients/week',
  `input_json` JSON COMMENT '输入参数',
  `result_json` JSON COMMENT '推荐结果',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE SET NULL,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_type` (`recommend_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### feedback 反馈表

```sql
CREATE TABLE `feedback` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT,
  `content` TEXT NOT NULL,
  `contact` VARCHAR(100),
  `status` TINYINT DEFAULT 0 COMMENT '0-待处理 1-已处理',
  `reply` TEXT COMMENT '回复内容',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE SET NULL,
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### banners 轮播图表

```sql
CREATE TABLE `banners` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `title` VARCHAR(100),
  `image` VARCHAR(500) NOT NULL,
  `link_type` VARCHAR(20) COMMENT 'recipe/category/url',
  `link_value` VARCHAR(500),
  `sort` INT DEFAULT 0,
  `status` TINYINT DEFAULT 1 COMMENT '0-禁用 1-启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX `idx_sort_status` (`sort`, `status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### admin_users 管理员表

```sql
CREATE TABLE `admin_users` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) UNIQUE NOT NULL,
  `password_hash` VARCHAR(255) NOT NULL,
  `role` VARCHAR(20) DEFAULT 'admin',
  `status` TINYINT DEFAULT 1 COMMENT '0-禁用 1-启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX `idx_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 四、配置管理

### 4.1 环境变量 (.env.example)

```bash
# 应用配置
APP_NAME=菜单推荐
APP_VERSION=1.0.0
DEBUG=true

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=menu_recommend

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT 配置
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# 跨域配置
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174"]

# 上传配置
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760

# AI 配置（预留）
AI_API_KEY=
AI_API_URL=
```

### 4.2 配置文件 (config.py)

```python
from pydantic_settings import BaseSettings
from typing import List
import json


class Settings(BaseSettings):
    # 应用配置
    APP_NAME: str = "菜单推荐"
    APP_VERSION: str = "1.0.0"
    DEBUG: bool = True

    # 服务器配置
    HOST: str = "0.0.0.0"
    PORT: int = 8000

    # 数据库配置
    DB_HOST: str = "localhost"
    DB_PORT: int = 3306
    DB_USER: str = "root"
    DB_PASSWORD: str = ""
    DB_NAME: str = "menu_recommend"

    # Redis 配置
    REDIS_HOST: str = "localhost"
    REDIS_PORT: int = 6379
    REDIS_PASSWORD: str = ""
    REDIS_DB: int = 0

    # JWT 配置
    JWT_SECRET_KEY: str = "your-secret-key"
    JWT_ALGORITHM: str = "HS256"
    JWT_ACCESS_TOKEN_EXPIRE_MINUTES: int = 1440

    # 跨域配置
    CORS_ORIGINS: List[str] = ["http://localhost:5173", "http://localhost:5174"]

    # 上传配置
    UPLOAD_DIR: str = "./uploads"
    MAX_UPLOAD_SIZE: int = 10 * 1024 * 1024  # 10MB

    @property
    def DATABASE_URL(self) -> str:
        return f"mysql+pymysql://{self.DB_USER}:{self.DB_PASSWORD}@{self.DB_HOST}:{self.DB_PORT}/{self.DB_NAME}?charset=utf8mb4"

    @property
    def REDIS_URL(self) -> str:
        return f"redis://:{self.REDIS_PASSWORD}@{self.REDIS_HOST}:{self.REDIS_PORT}/{self.REDIS_DB}"

    class Config:
        env_file = ".env"
        case_sensitive = True


settings = Settings()
```

---

## 五、API 接口开发任务

### 5.1 统一响应格式

```python
# schemas/base.py
from pydantic import BaseModel
from typing import Any, Optional


class ResponseBase(BaseModel):
    code: int = 0
    message: str = "success"
    data: Any = None


class PageResponse(BaseModel):
    code: int = 0
    message: str = "success"
    data: dict = None
    # data 结构: { list: [], total: 0, page: 1, page_size: 10 }
```

### 5.2 用户端接口

#### 认证模块 (api/v1/auth.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| POST | /api/auth/register | 用户注册 | username, email?, phone?, password |
| POST | /api/auth/login | 用户登录 | username, password |

**接口详情**：

```python
# POST /api/auth/register
# 请求
{
    "username": "testuser",
    "email": "test@example.com",
    "phone": "13800138000",
    "password": "password123"
}

# 响应
{
    "code": 0,
    "message": "success",
    "data": {
        "token": "eyJ...",
        "user": {
            "id": 1,
            "username": "testuser",
            "nickname": "testuser"
        }
    }
}

# POST /api/auth/login
# 请求
{
    "username": "testuser",
    "password": "password123"
}

# 响应
{
    "code": 0,
    "message": "success",
    "data": {
        "token": "eyJ...",
        "user": {
            "id": 1,
            "username": "testuser",
            "nickname": "testuser",
            "avatar": null
        }
    }
}
```

---

#### 用户模块 (api/v1/user.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/user/profile | 获取用户信息 | - |
| PUT | /api/user/profile | 更新用户信息 | nickname, avatar, gender |
| GET | /api/user/preferences | 获取偏好设置 | - |
| PUT | /api/user/preferences | 更新偏好设置 | taste_preference, health_goal, ... |
| GET | /api/user/favorites | 获取收藏列表 | page, page_size |
| GET | /api/user/menus | 获取我的菜单 | page, page_size |

---

#### 首页模块 (api/v1/home.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/home | 获取首页数据 | - |

**响应结构**：
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "banners": [...],
        "today_recommend": {...},
        "quick_meals": {
            "breakfast": [...],
            "lunch": [...],
            "dinner": [...]
        },
        "hot_recipes": [...],
        "categories": [...]
    }
}
```

---

#### 菜谱模块 (api/v1/recipe.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/recipes | 菜谱列表 | keyword, category_id, taste, cook_time, difficulty, health_tags, page, page_size |
| GET | /api/recipes/{id} | 菜谱详情 | - |
| POST | /api/recipes/{id}/favorite | 收藏菜谱 | - |
| DELETE | /api/recipes/{id}/favorite | 取消收藏 | - |

**菜谱列表响应**：
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "list": [
            {
                "id": 1,
                "title": "红烧肉",
                "cover": "https://...",
                "cook_time": 60,
                "difficulty": "中等",
                "taste": "咸鲜",
                "favorite_count": 128
            }
        ],
        "total": 100,
        "page": 1,
        "page_size": 10
    }
}
```

**菜谱详情响应**：
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "id": 1,
        "title": "红烧肉",
        "cover": "https://...",
        "description": "经典家常菜...",
        "category": {"id": 1, "name": "家常菜"},
        "cook_time": 60,
        "difficulty": "中等",
        "people_count": 4,
        "taste": "咸鲜",
        "health_tags": ["高蛋白"],
        "ingredients": [
            {"name": "五花肉", "amount": "500g"},
            {"name": "生姜", "amount": "3片"}
        ],
        "seasonings": [
            {"name": "生抽", "amount": "2勺"},
            {"name": "老抽", "amount": "1勺"}
        ],
        "steps": [
            {"step": 1, "desc": "五花肉切块...", "image": null},
            {"step": 2, "desc": "焯水去血沫...", "image": null}
        ],
        "tips": "焯水时加入料酒可以去腥",
        "nutrition": {
            "calories": 500,
            "protein": 30,
            "fat": 40,
            "carbs": 10
        },
        "is_favorited": false
    }
}
```

---

#### 推荐模块 (api/v1/recommend.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| POST | /api/recommend/menu | 智能菜单推荐 | people_count, meal_type, taste_preference, health_goal, avoid_ingredients, existing_ingredients, cook_time_preference |
| POST | /api/recommend/by-ingredients | 按食材推荐 | ingredients |
| POST | /api/recommend/week-menu | 一周菜单 | people_count, taste_preference, health_goal |

**菜单推荐请求**：
```json
{
    "people_count": 3,
    "meal_type": "lunch",
    "taste_preference": ["咸鲜", "清淡"],
    "health_goal": "减脂",
    "avoid_ingredients": ["香菜"],
    "existing_ingredients": ["鸡胸肉", "西兰花"],
    "cook_time_preference": "medium"
}
```

**菜单推荐响应**：
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "menu_name": "减脂增肌午餐",
        "reason": "鸡胸肉高蛋白低脂，搭配西兰花营养均衡...",
        "dishes": [
            {
                "name": "香煎鸡胸肉",
                "type": "主菜",
                "cook_time": 20,
                "difficulty": "简单",
                "ingredients": ["鸡胸肉", "黑胡椒", "橄榄油"],
                "steps_summary": "腌制后煎至两面金黄"
            },
            {
                "name": "蒜蓉西兰花",
                "type": "配菜",
                "cook_time": 10,
                "difficulty": "简单",
                "ingredients": ["西兰花", "大蒜"],
                "steps_summary": "焯水后蒜蓉翻炒"
            }
        ],
        "shopping_list": ["黑胡椒", "橄榄油", "大蒜"]
    }
}
```

---

#### 购物清单模块 (api/v1/shopping.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/shopping-list | 获取购物清单 | - |
| POST | /api/shopping-list | 创建购物清单 | name, items |
| PUT | /api/shopping-list/{id} | 更新购物清单 | name?, items? |
| DELETE | /api/shopping-list/{id} | 删除购物清单 | - |

---

#### 反馈模块 (api/v1/feedback.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| POST | /api/feedback | 提交反馈 | content, contact? |

---

#### 轮播图模块 (api/v1/banner.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/banners | 获取轮播图列表 | - |

---

### 5.3 管理后台接口

#### 后台认证 (api/admin/auth.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| POST | /api/admin/login | 管理员登录 | username, password |

---

#### 数据概览 (api/admin/dashboard.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/dashboard | 获取统计数据 | - |

**响应**：
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_count": 1000,
        "recipe_count": 500,
        "recommend_count": 5000,
        "favorite_count": 3000,
        "user_trend": [...],
        "recommend_trend": [...],
        "hot_recipes": [...]
    }
}
```

---

#### 菜谱管理 (api/admin/recipe.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/recipes | 菜谱列表 | keyword, category_id, status, page, page_size |
| POST | /api/admin/recipes | 新增菜谱 | 完整菜谱字段 |
| PUT | /api/admin/recipes/{id} | 编辑菜谱 | 完整菜谱字段 |
| DELETE | /api/admin/recipes/{id} | 删除菜谱 | - |

---

#### 分类管理 (api/admin/category.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/categories | 分类列表 | - |
| POST | /api/admin/categories | 新增分类 | name, icon, sort |
| PUT | /api/admin/categories/{id} | 编辑分类 | name, icon, sort, status |
| DELETE | /api/admin/categories/{id} | 删除分类 | - |

---

#### 食材管理 (api/admin/ingredient.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/ingredients | 食材列表 | keyword, category, page, page_size |
| POST | /api/admin/ingredients | 新增食材 | name, category, image, nutrition_info |
| PUT | /api/admin/ingredients/{id} | 编辑食材 | name, category, image, nutrition_info |
| DELETE | /api/admin/ingredients/{id} | 删除食材 | - |

---

#### 轮播图管理 (api/admin/banner.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/banners | 轮播图列表 | - |
| POST | /api/admin/banners | 新增轮播图 | title, image, link_type, link_value, sort |
| PUT | /api/admin/banners/{id} | 编辑轮播图 | title, image, link_type, link_value, sort, status |
| DELETE | /api/admin/banners/{id} | 删除轮播图 | - |

---

#### 用户管理 (api/admin/user.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/users | 用户列表 | keyword, page, page_size |
| GET | /api/admin/users/{id} | 用户详情 | - |

---

#### 推荐记录 (api/admin/recommend_log.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/recommend-logs | 推荐记录列表 | recommend_type, page, page_size |

---

#### 反馈管理 (api/admin/feedback.py)

| 方法 | 路径 | 描述 | 参数 |
|------|------|------|------|
| GET | /api/admin/feedback | 反馈列表 | status, page, page_size |
| PUT | /api/admin/feedback/{id} | 处理反馈 | status, reply |

---

## 六、核心服务开发任务

### 6.1 认证服务 (services/auth_service.py)

**功能**：
- 用户注册（密码加密）
- 用户登录（验证密码）
- 生成 JWT Token
- 验证 JWT Token

**依赖**：
- `passlib` 密码加密
- `python-jose` JWT 处理

**关键实现**：

```python
from passlib.context import CryptContext
from jose import jwt
from datetime import datetime, timedelta

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)

def create_access_token(data: dict, expires_delta: timedelta = None):
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=1440))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, settings.JWT_SECRET_KEY, algorithm=settings.JWT_ALGORITHM)
```

---

### 6.2 推荐服务 (services/recommend_service.py)

**功能**：
- 基于规则的菜单推荐
- 按食材推荐
- 一周菜单生成

**推荐算法**：

#### 智能菜单推荐

```python
def recommend_menu(params: RecommendParams) -> dict:
    """
    推荐算法流程：
    1. 根据用餐类型筛选菜谱
    2. 过滤忌口食材
    3. 根据口味偏好评分
    4. 根据饮食目标评分
    5. 根据已有食材匹配度评分
    6. 根据烹饪时间筛选
    7. 综合评分排序
    8. 选择主菜、配菜、汤、主食搭配
    9. 生成购物清单
    """
    pass
```

**评分规则**：

| 因素 | 权重 | 说明 |
|------|------|------|
| 食材匹配度 | 30% | 已有食材占比越高分越高 |
| 口味偏好 | 20% | 匹配用户口味偏好 |
| 饮食目标 | 20% | 匹配健康标签 |
| 烹饪时间 | 15% | 符合用户时间偏好 |
| 热门权重 | 15% | 收藏数和浏览量 |

#### 按食材推荐

```python
def recommend_by_ingredients(ingredients: list) -> list:
    """
    1. 查询包含这些食材的菜谱
    2. 计算食材匹配度（匹配食材数 / 总食材数）
    3. 按匹配度排序
    4. 标记缺少的食材
    """
    pass
```

#### 一周菜单生成

```python
def generate_week_menu(params: WeekMenuParams) -> list:
    """
    1. 生成7天菜单
    2. 每天包含早餐、午餐、晚餐
    3. 确保菜品不重复
    4. 营养均衡搭配
    """
    pass
```

---

### 6.3 AI 服务 (services/ai_service.py) - 预留

**功能**：
- 调用 AI API 进行智能推荐
- 解析 AI 返回的 JSON

**接口**：

```python
async def ai_recommend_menu(params: dict) -> dict:
    """
    调用 AI 接口获取推荐

    输入：
    - 用餐人数
    - 用餐时间
    - 口味偏好
    - 饮食目标
    - 已有食材
    - 忌口食材
    - 期望烹饪时间

    输出：
    {
        "menu_name": "推荐菜单名称",
        "reason": "推荐理由",
        "dishes": [...],
        "shopping_list": [...]
    }
    """
    pass
```

---

### 6.4 缓存服务 (utils/cache.py)

**功能**：
- Redis 连接管理
- 缓存首页数据
- 缓存热门菜谱
- 缓存分类数据

**缓存策略**：

| 数据 | 缓存时间 | 说明 |
|------|----------|------|
| 首页数据 | 10分钟 | 包含多个查询，需要缓存 |
| 热门菜谱 | 30分钟 | 变化不频繁 |
| 分类列表 | 1小时 | 很少变化 |
| 菜谱详情 | 5分钟 | 需要实时收藏数 |

**实现**：

```python
import redis.asyncio as redis
from app.config import settings

redis_client = redis.from_url(settings.REDIS_URL, decode_responses=True)

async def get_cache(key: str):
    return await redis_client.get(key)

async def set_cache(key: str, value: str, expire: int = 300):
    await redis_client.set(key, value, ex=expire)

async def delete_cache(key: str):
    await redis_client.delete(key)
```

---

## 七、中间件和依赖

### 7.1 认证依赖 (dependencies.py)

```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from jose import jwt, JWTError
from app.config import settings

security = HTTPBearer()

async def get_current_user(credentials: HTTPAuthorizationCredentials = Depends(security)):
    try:
        payload = jwt.decode(
            credentials.credentials,
            settings.JWT_SECRET_KEY,
            algorithms=[settings.JWT_ALGORITHM]
        )
        user_id = payload.get("sub")
        if user_id is None:
            raise HTTPException(status_code=401, detail="Invalid token")
        return {"user_id": int(user_id)}
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")


async def get_optional_user(credentials: HTTPAuthorizationCredentials = Depends(security)):
    """可选认证，未登录返回 None"""
    try:
        return await get_current_user(credentials)
    except:
        return None
```

### 7.2 管理员认证

```python
async def get_admin_user(credentials: HTTPAuthorizationCredentials = Depends(security)):
    try:
        payload = jwt.decode(
            credentials.credentials,
            settings.JWT_SECRET_KEY,
            algorithms=[settings.JWT_ALGORITHM]
        )
        user_id = payload.get("sub")
        role = payload.get("role")
        if user_id is None or role != "admin":
            raise HTTPException(status_code=403, detail="Admin access required")
        return {"user_id": int(user_id)}
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")
```

### 7.3 CORS 中间件

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 7.4 异常处理

```python
from fastapi import Request
from fastapi.responses import JSONResponse

class AppException(Exception):
    def __init__(self, code: int, message: str):
        self.code = code
        self.message = message

@app.exception_handler(AppException)
async def app_exception_handler(request: Request, exc: AppException):
    return JSONResponse(
        status_code=200,
        content={"code": exc.code, "message": exc.message, "data": None}
    )

@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content={"code": -1, "message": "Internal Server Error", "data": None}
    )
```

---

## 八、测试任务

### 8.1 测试环境

```python
# tests/conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from app.main import app
from app.database import Base, get_db

SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(SQLALCHEMY_DATABASE_URL, connect_args={"check_same_thread": False})
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


def override_get_db():
    db = TestingSessionLocal()
    try:
        yield db
    finally:
        db.close()


app.dependency_overrides[get_db] = override_get_db
client = TestClient(app)


@pytest.fixture(autouse=True)
def setup_database():
    Base.metadata.create_all(bind=engine)
    yield
    Base.metadata.drop_all(bind=engine)
```

### 8.2 测试用例

| 测试文件 | 测试内容 |
|----------|----------|
| test_auth.py | 注册、登录、Token 验证 |
| test_recipe.py | 菜谱 CRUD、列表查询、详情 |
| test_recommend.py | 菜单推荐、食材推荐 |
| test_shopping.py | 购物清单 CRUD |
| test_favorite.py | 收藏、取消收藏 |

**示例测试**：

```python
# tests/test_auth.py
def test_register():
    response = client.post("/api/auth/register", json={
        "username": "testuser",
        "password": "password123",
        "email": "test@example.com"
    })
    assert response.status_code == 200
    data = response.json()
    assert data["code"] == 0
    assert "token" in data["data"]


def test_login():
    # 先注册
    client.post("/api/auth/register", json={
        "username": "testuser",
        "password": "password123"
    })
    # 登录
    response = client.post("/api/auth/login", json={
        "username": "testuser",
        "password": "password123"
    })
    assert response.status_code == 200
    data = response.json()
    assert data["code"] == 0
    assert "token" in data["data"]
```

---

## 九、Mock 数据

### 9.1 菜谱分类

```python
CATEGORIES = [
    {"name": "家常菜", "icon": "home-cooking", "sort": 1},
    {"name": "快手菜", "icon": "quick", "sort": 2},
    {"name": "早餐", "icon": "breakfast", "sort": 3},
    {"name": "汤羹", "icon": "soup", "sort": 4},
    {"name": "主食", "icon": "staple", "sort": 5},
    {"name": "凉菜", "icon": "cold-dish", "sort": 6},
    {"name": "烘焙", "icon": "baking", "sort": 7},
    {"name": "饮品", "icon": "drink", "sort": 8},
]
```

### 9.2 示例菜谱

```python
RECIPES = [
    {
        "title": "番茄炒蛋",
        "description": "经典家常菜，简单快手，营养美味",
        "category_id": 1,
        "cook_time": 15,
        "difficulty": "简单",
        "people_count": 2,
        "taste": "咸鲜",
        "health_tags": ["快手", "低脂"],
        "ingredients": [
            {"name": "鸡蛋", "amount": "3个"},
            {"name": "番茄", "amount": "2个"}
        ],
        "seasonings": [
            {"name": "盐", "amount": "适量"},
            {"name": "糖", "amount": "少许"},
            {"name": "葱", "amount": "适量"}
        ],
        "steps": [
            {"step": 1, "desc": "鸡蛋打散，番茄切块"},
            {"step": 2, "desc": "热锅凉油，倒入蛋液炒熟盛出"},
            {"step": 3, "desc": "锅中留底油，放入番茄翻炒出汁"},
            {"step": 4, "desc": "加入鸡蛋，调入盐和糖，翻炒均匀"}
        ],
        "tips": "番茄用开水烫一下更容易去皮",
        "nutrition": {"calories": 150, "protein": 12, "fat": 8, "carbs": 10}
    },
    # ... 更多菜谱
]
```

### 9.3 食材数据

```python
INGREDIENTS = [
    # 蔬菜类
    {"name": "番茄", "category": "蔬菜"},
    {"name": "黄瓜", "category": "蔬菜"},
    {"name": "土豆", "category": "蔬菜"},
    {"name": "茄子", "category": "蔬菜"},
    {"name": "青椒", "category": "蔬菜"},
    {"name": "白菜", "category": "蔬菜"},
    {"name": "西兰花", "category": "蔬菜"},
    {"name": "胡萝卜", "category": "蔬菜"},
    # 肉类
    {"name": "猪肉", "category": "肉类"},
    {"name": "牛肉", "category": "肉类"},
    {"name": "鸡肉", "category": "肉类"},
    {"name": "鸡胸肉", "category": "肉类"},
    # 海鲜类
    {"name": "虾", "category": "海鲜"},
    {"name": "鱼", "category": "海鲜"},
    # 蛋奶类
    {"name": "鸡蛋", "category": "蛋奶"},
    {"name": "牛奶", "category": "蛋奶"},
    # 调料类
    {"name": "盐", "category": "调料"},
    {"name": "生抽", "category": "调料"},
    {"name": "老抽", "category": "调料"},
    {"name": "料酒", "category": "调料"},
    {"name": "醋", "category": "调料"},
    {"name": "糖", "category": "调料"},
]
```

---

## 十、开发优先级

### P0 - 基础架构（第1周）

1. 项目初始化和配置
2. 数据库连接和 ORM 模型
3. Redis 连接
4. 统一响应和异常处理
5. JWT 认证中间件

### P1 - 核心接口（第2周）

6. 用户注册登录
7. 菜谱列表和详情
8. 分类接口
9. 首页接口

### P2 - 推荐功能（第3周）

10. 推荐算法实现
11. 菜单推荐接口
12. 按食材推荐接口
13. 一周菜单接口

### P3 - 辅助功能（第4周）

14. 收藏功能
15. 购物清单
16. 偏好设置
17. 反馈功能

### P4 - 管理后台（第5周）

18. 管理员认证
19. Dashboard 统计
20. 菜谱管理 CRUD
21. 分类、食材、轮播图管理
22. 用户、推荐记录、反馈管理

### P5 - 优化完善（第6周）

23. Redis 缓存优化
24. 单元测试
25. API 文档完善
26. 性能优化

---

## 十一、部署配置

### 11.1 Docker 部署

```dockerfile
# Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - mysql
      - redis
    volumes:
      - ./uploads:/app/uploads

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: menu_recommend
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  mysql_data:
```

### 11.2 启动命令

```bash
# 开发环境
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 生产环境
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

---

## 十二、验收标准

### 功能验收

- [ ] 所有 API 接口正常返回
- [ ] 认证和鉴权正常
- [ ] 推荐算法结果合理
- [ ] 缓存生效
- [ ] 数据库操作正确

### 性能验收

- [ ] 接口响应时间 < 200ms（不含推荐算法）
- [ ] 推荐接口响应时间 < 2s
- [ ] 支持并发 100 用户

### 代码质量

- [ ] 类型注解完整
- [ ] 异常处理完善
- [ ] 日志记录完善
- [ ] 测试覆盖核心逻辑

### 文档验收

- [ ] API 文档自动生成（Swagger）
- [ ] 接口说明清晰
- [ ] 部署文档完整
